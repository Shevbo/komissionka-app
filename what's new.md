# Лог изменений приложения Комиссионка

## Структура записи (соблюдать обязательно)

1. **Дата время >** Краткий запрос на изменение (от пользователя)
2. **Дата время >** Кратко что сделано по каждому приложению/сервису:

**2.1 В приложении Комиссионка (web)**  
[описание изменений, какие новые объекты в репозитории, сколько заменено строк из скольких строк ключевого функционала всего, какие документы/инструкции созданы/обновлены с кликабельными ссылками, версия была → версия стала]

**2.2 В сервисе Агент к модели ИИ**  
[описание изменений, какие новые объекты в репозитории, сколько заменено строк из скольких строк ключевого функционала всего, какие документы/инструкции созданы/обновлены с кликабельными ссылками, версия была → версия стала]

**2.3 В сервисе Телеграм-Бот**  
[описание изменений, какие новые объекты в репозитории, сколько заменено строк из скольких строк ключевого функционала всего, какие документы/инструкции созданы/обновлены с кликабельными ссылками, версия была → версия стала]

**2.4 PWA** (мобильная веб-версия — часть web-приложения)  
[описание изменений или «Без изменений»]

Подсчёт строк core: `npx tsx scripts/count-core-lines.ts`

---

## Записи

### ______***UPDATE***________

**22.03.2026 >** Прогон каталога: таймаут хода = таймаут агента; дефолт 20 мин (не «висит 10 мин» из-за рассинхрона)

**2.1 В приложении Комиссионка (web)**  
Раннер `POST /api/admin/test-cases/[id]/run` передаёт в агент **`timeoutMs`** (тот же бюджет, что и `fetch`). Дефолт **`TEST_RUN_AGENT_FETCH_TIMEOUT_MS`**: **1200000** мс (20 мин, макс. 45 мин в env). Файлы: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts), [src/lib/test-run-config.ts](src/lib/test-run-config.ts). Документация: [docs/manual/test-catalog-architecture.md](docs/manual/test-catalog-architecture.md) §6.3, вер. 1.0.11. Core приложения (count-core-lines): **14773** строки. app v1.19.0 → v1.19.1 (патч).

**2.2 В сервисе Агент к модели ИИ**  
**`POST /run`**: опциональное тело **`timeoutMs`** (30 с … 45 мин) передаётся в **`runAgent`**, чтобы внутренний `withTimeout` совпадал с клиентом раннера. Файл: [agent/serve.ts](agent/serve.ts). Core агента: **4008** строк. agent v1.7.15 → v1.7.16 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**22.03.2026 >** Каталог тестов: промпт для `test-runner` — без «подтвердите удаление» на первом ходе

**2.1 В приложении Комиссионка (web)**  
Без изменений по коду прогона. app v1.19.0.

**2.2 В сервисе Агент к модели ИИ**  
При **`environment: "test-runner"`** к системному промпту **добавляется блок**: не задавать цепочку уточняющих вопросов в автоматическом прогоне; выполнять действие (в т.ч. деструктивное по условию промпта) и явно описывать результат в финальном ответе — чтобы сценарии вроде «удали карточку Fender Stratocaster» не останавливались на «подтвердите удаление». Файл: [agent/core.ts](agent/core.ts). Документация: [docs/manual/test-catalog-architecture.md](docs/manual/test-catalog-architecture.md) §6.3, вер. 1.0.10. Core агента (count-core-lines): **3990** строк. agent v1.7.14 → v1.7.15 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**05.03.2026 >** Каталог тестов: копирование тест-кейса и редактирование (API + UI)

**2.1 В приложении Комиссионка (web)**  
**Копирование:** `POST /api/admin/test-cases` с телом `{ "copyFromId": "<uuid>" }` — новый кейс с `number = max(number)+1`, заголовок с суффиксом « (копия)», сброс полей обогащения спецификации ИИ. **Редактирование:** единая форма создания/редактирования в [src/components/AdminTestCatalogTab.tsx](src/components/AdminTestCatalogTab.tsx); `PUT /api/admin/test-cases` — обновление полей кейса, в т.ч. **`number`** с проверкой уникальности (**409** при конфликте). В таблице и карточке кейса — кнопки **копировать** и **редактировать**. Файл API: [src/app/api/admin/test-cases/route.ts](src/app/api/admin/test-cases/route.ts). Документация: [docs/manual/test-catalog-architecture.md](docs/manual/test-catalog-architecture.md) §6.1, §7.1, вер. 1.0.9. Core приложения (count-core-lines): **14770** строк. app v1.18.11 → v1.19.0 (минор).

**2.2–2.4**  
Без изменений. agent v1.7.14, tgbot v1.1.0.

**05.03.2026 >** Багфикс: ложный success по UUID — товар уже отсутствовал в БД до прогона (прогон 36 и др.)

**2.1 В приложении Комиссионка (web)**  
Для `scope=agent` и `expectedText` в виде UUID (проверка удаления в `items`): успех **`dbItemDeletedById`** только если запись **была в БД до старта прогона** и **исчезла к концу**; иначе `!item` давало ложный успех без второго хода диалога. Добавлена проверка **`expectedItemNotPresentAtRunStart`** (прогон без вызова агента, если товара уже нет). В успехе в `diagnostics`: `itemExistedAtRunStart`, `uuidDeleteVerified`. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). Документация: [docs/manual/test-catalog-architecture.md](docs/manual/test-catalog-architecture.md) §6.3, вер. 1.0.8. Core приложения: **14713** строк (count-core-lines). app v1.18.10 → v1.18.11 (патч).

**2.2–2.4**  
Без изменений. agent v1.7.14, tgbot v1.1.0.

**05.03.2026 >** Багфикс: путаница «итерация 40» — внутренний цикл LLM+tools, а не ход диалога раннера; правило «диагностика от пользователя = ошибка»

**2.1 В приложении Комиссионка (web)**  
В `diagnostics` прогона каталога тестов: **`runnerDialogTurn`** (раунд имитации пользователя), **`stepsMeaning`** расшифровка; поле `turn` оставлено как дубликат с пометкой в коде. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). Документация: [docs/manual/test-catalog-architecture.md](docs/manual/test-catalog-architecture.md) §6.3, вер. 1.0.7. Правило для Cursor: [.cursor/rules/user-diagnostics.mdc](.cursor/rules/user-diagnostics.mdc). Core приложения: **14677** строк. app v1.18.9 → v1.18.10 (патч).

**2.2 В сервисе Агент к модели ИИ**  
В **steps** шаги `llm` переименованы: вместо «Обращение к модели (итерация N)» — **«LLM+инструменты: внутренний шаг N/70»** с пояснением, что это не ход чата/раннера. Стартовый шаг: «Запрос к модели (старт внутреннего цикла)». Файл: [agent/core.ts](agent/core.ts). Core агента: **3979** строк. agent v1.7.13 → v1.7.14 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**05.03.2026 >** Багфикс: каталог тестов scope=agent — имитация ответов пользователя до результата (не только при «вопросе»)

**2.1 В приложении Комиссионка (web)**  
Раннер `POST /api/admin/test-cases/[id]/run` для `scope=agent`: **после каждого ответа агента**, если критерий успеха не выполнен, **всегда** планируется следующий имитированный ответ пользователя (эскалация по шаблонам или `parameters.userResponses`), без эвристики «есть ли вопрос в тексте». Лимиты: **`TEST_RUN_MAX_CHAT_TURNS`** (по умолчанию **32**), **`TEST_RUN_AGENT_FETCH_TIMEOUT_MS`** (по умолчанию **10 мин** на один POST к агенту), **`TEST_RUN_AGENT_SESSION_MAX_MS`** (по умолчанию **90 мин** на весь сеанс). Диагностика при исчерпании ходов: `simulationTurnsExhausted`. Файлы: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts), [src/lib/test-run-config.ts](src/lib/test-run-config.ts). Удалён недостижимый мёртвый код после `return` в том же маршруте. Документация: [docs/manual/test-catalog-architecture.md](docs/manual/test-catalog-architecture.md) §6.3, вер. 1.0.6. Всего core приложения: **14668** строк; core агента: **3974** строки (count-core-lines). app v1.18.8 → v1.18.9 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Для **`environment === "test-runner"`** в `runAgentCore` всегда **`forceSystemPrompt: true`** (вместе с `forceFresh`), чтобы на каждом ходе многоходового прогона в запрос к LLM попадали правила и инструменты. Файл: [agent/contract.ts](agent/contract.ts). agent v1.7.12 → v1.7.13 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**05.03.2026 >** Экспорт прогона для ИИ: API + кнопки на интерактиве (незакоммиченное ранее)

**2.1 В приложении Комиссионка (web)**  
Добавлены **`GET /api/admin/test-cases/runs/export/[runId]`** (скачивание JSON) и **`POST …/export/[runId]/webhook`** (прокидывание того же JSON на URL из `TEST_RUN_EXPORT_WEBHOOK_URL`). Модуль [src/lib/test-run-export.ts](src/lib/test-run-export.ts). На странице [src/app/admin/test-runs/[runId]/interactive/page.tsx](src/app/admin/test-runs/[runId]/interactive/page.tsx) — кнопки **«JSON для ИИ»** и **«На вебхук»**. Документация: [docs/manual/test-catalog-architecture.md](docs/manual/test-catalog-architecture.md) §6.4. Core приложения (count-core-lines): **14806** строк. app v1.18.7 → v1.18.8 (минор).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.12.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**05.03.2026 >** Где кнопка экспорта прогона — перенос «Скачать JSON для ИИ» / «На вебхук» под заголовок диалога

**2.1 В приложении Комиссионка (web)**  
В диалоге **деталей прогона** кнопки **«Скачать JSON для ИИ»** и **«Отправить на вебхук»** перенесены в **шапку** (`DialogHeader`) сразу под заголовком «Прогон #…», чтобы они были видны без прокрутки и не терялись на старых скриншотах/узком окне. Дубликат в середине карточки убран. Файл: [src/components/AdminTestCatalogTab.tsx](src/components/AdminTestCatalogTab.tsx). Документация: [docs/manual/test-catalog-architecture.md](docs/manual/test-catalog-architecture.md) — §6.4, вер. 1.0.5. Всего core приложения (count-core-lines): **14806** строк. app v1.18.6 → v1.18.7 (патч, UX).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.12.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**21.03.2026 >** Багфикс: прогоны каталога тестов (№31) — полное отключение radical DEV-guard для test-runner

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.18.8.

**2.2 В сервисе Агент к модели ИИ**  
При `environment === "test-runner"` блок **`tool_calls` больше не проходит** через radical DEV-guard (эвристика уточнений). Раннер сам шлёт имитацию пользователя; guard остаётся для админки/Telegram и др. В `runAgent` в `runAgentCore` пробрасывается **`environment`**. Файлы: [agent/core.ts](agent/core.ts), [agent/contract.ts](agent/contract.ts). Документация: [docs/manual/test-catalog-architecture.md](docs/manual/test-catalog-architecture.md) — как смотреть полный прогон через detail API и `agentLogId`. Core агента (count-core-lines): 3973 строки. agent v1.7.11 → v1.7.12 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**21.03.2026 >** Багфикс: прогон каталога тестов (№27) — второй ход снова без эффекта из-за DEV guard

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.18.6.

**2.2 В сервисе Агент к модели ИИ**  
В режиме разработки **radical guard** больше не блокирует tool_calls, если в **переданной** `history` уже есть реплика `assistant` (ответ на предыдущий HTTP-запрос): иначе при формулировке уточнения без `?`/ключевых слов эвристика считала, что «уточнений не было», и на втором ходе снова отбрасывались вызовы инструментов — раннер теста «не достигал» удаления. Файл: [agent/core.ts](agent/core.ts). Core агента (count-core-lines): 3966 строк. agent v1.7.10 → v1.7.11 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**21.03.2026 >** Багфикс: прогон каталога тестов — второй ход зависает / модель «не слышит» имитацию пользователя

**2.1 В приложении Комиссионка (web)**  
В теле `POST` к агенту из раннера добавлено явное **`disableCache: true`**. Исправлен проброс **`disableCache`** в **нестриминговом** `POST /run` агента (раньше поле из JSON игнорировалось — кэш мог оставаться активен). Файлы: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts), [agent/serve.ts](agent/serve.ts). app v1.18.5 → v1.18.6 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Перед сборкой сообщений для LLM из **истории чата** с реплик ассистента снимается **подвал отчёта** (`\n\n---\nМодель:…`), иначе во втором запросе в контекст попадали тысячи символов метрик вместо текста уточнений — Gemini получал «шум» и сценарий ломался. Функция [agent/lib/report-footer.ts](agent/lib/report-footer.ts) `stripAgentReportFooter`, использование в [agent/core.ts](agent/core.ts). Core агента (count-core-lines): 3963 строки. app core: 14386 строк. agent v1.7.9 → v1.7.10 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**05.03.2026 >** Багфикс: многоходовый прогон каталога тестов — ответ имитации пользователя не учитывался моделью из-за кэша агента

**2.1 В приложении Комиссионка (web)**  
Без изменений в этом коммите. app v1.18.5.

**2.2 В сервисе Агент к модели ИИ**  
В `runAgent` отключён поиск в `agent_prompt_cache`, если передана непустая `history` или `environment === "test-runner"` (раннер передаёт это из `POST /run`). В `findSimilar` добавлены фильтры по `environment` и `history_turns`, чтобы записи кэша не смешивали разные среды и глубину диалога. Файлы: [agent/contract.ts](agent/contract.ts), [agent/cache/index.ts](agent/cache/index.ts). Core агента (count-core-lines): 3961 строка. agent v1.7.8 → v1.7.9 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**05.03.2026 >** Багфикс: «вечный» статус running у прогона (часы без диалога) — авто-закрытие протухших прогонов

**2.1 В приложении Комиссионка (web)**  
Прогон мог оставаться `running` без `finished_at`, если процесс Next.js был перезапущен или HTTP-запрос раннера оборван: финальный `update` в БД не выполнялся. Добавлено:  
1) **Авто-финализация** записей `status=running`, у которых `started_at` старше порога (`TEST_RUN_STALE_AFTER_MS`, по умолчанию **2 ч**): при открытии списка тестов (`GET /api/admin/test-cases`), истории прогонов кейса или карточки прогона прогон переводится в **`failed`** с `diagnostics.staleRun` и проверкой `staleRunAutoFinalized`.  
2) Модуль [src/lib/test-run-config.ts](src/lib/test-run-config.ts): безопасные границы для `AGENT_FETCH_TIMEOUT_MS`, `TEST_RUN_MAX_MS`, `TEST_API_FETCH_TIMEOUT_MS` (защита от пустого env / некорректных чисел).  
3) Для **scope=api** внутренний `fetch` к приложению выполняется с **таймаутом** (`fetchWithTimeoutRace`, по умолчанию 120 с через `TEST_API_FETCH_TIMEOUT_MS`).  
Файлы: [src/lib/test-run-config.ts](src/lib/test-run-config.ts), [src/app/api/admin/test-cases/route.ts](src/app/api/admin/test-cases/route.ts), [src/app/api/admin/test-cases/runs/[testCaseId]/route.ts](src/app/api/admin/test-cases/runs/[testCaseId]/route.ts), [src/app/api/admin/test-cases/runs/detail/[runId]/route.ts](src/app/api/admin/test-cases/runs/detail/[runId]/route.ts), [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). Всего core приложения (count-core-lines): 14384 строк. app v1.18.4 → v1.18.5 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**05.03.2026 >** Багфикс: при ошибке/таймауте агента интерактив не теряет последний ход в `conversation_log`

**2.1 В приложении Комиссионка (web)**  
В `POST /api/admin/test-cases/[id]/run` при исключении до успешного ответа агента в `catch` в `chatHistory` дописываются последний пользовательский промпт и строка ассистента с текстом ошибки. Раньше финальный `update` мог перезаписать БД «пустым» `chatHistory` и стереть уже сохранённый pending‑диалог («ожидание ответа»), из‑за чего интерактив оставался пустым после долгого `running`. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). Всего core приложения (count-core-lines): 14212 строк. app v1.18.3 → v1.18.4 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**20.03.2026 >** Фича: остановка зависших прогонов и интерактивный live‑просмотр имитации чата

**2.1 В приложении Комиссионка (web)**  
Добавлены новые возможности для каталога тестов:
1) Кнопка **«Остановить»** для активных прогонов (`running/pending`) в истории и в карточке прогона.  
2) API отмены `POST /api/admin/test-cases/runs/cancel/[runId]`, который прерывает выполнение и выставляет статус `cancelled` с диагностикой «прервано админом».  
3) Кнопка **«Посмотреть интерактив»** — открывает новую вкладку `/admin/test-runs/[runId]/interactive`, где админ в режиме read‑only видит live‑имитацию диалога (polling карточки прогона каждые 2с).  
4) В раннере прогона добавлена регистрация/abort `AbortController` по runId, чтобы кнопка остановки реально прерывала активный вызов агента.  
Файлы: [src/components/AdminTestCatalogTab.tsx](src/components/AdminTestCatalogTab.tsx), [src/app/api/admin/test-cases/runs/cancel/[runId]/route.ts](src/app/api/admin/test-cases/runs/cancel/[runId]/route.ts), [src/app/admin/test-runs/[runId]/interactive/page.tsx](src/app/admin/test-runs/[runId]/interactive/page.tsx), [src/lib/test-run-control.ts](src/lib/test-run-control.ts), [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.17.4 → v1.18.0 (минор).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**20.03.2026 >** Багфикс: `Посмотреть интерактив` теперь показывает диалог во время running

**2.1 В приложении Комиссионка (web)**  
В раннере `POST /api/admin/test-cases/[id]/run` добавлено инкрементальное сохранение прогресса на каждом ходе: `conversation_log`, `steps`, `agent_log_id`, `diagnostics(phase/turn)` обновляются в БД сразу, не только в конце прогона. Благодаря этому страница `/admin/test-runs/[runId]/interactive` показывает сообщения в режиме live при статусе `running`. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.18.0 → v1.18.1 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**20.03.2026 >** Багфикс: имитация диалога не обрывается на формулировках "уточняющий вопрос / согласны ли"

**2.1 В приложении Комиссионка (web)**  
Усилен детектор уточняющих вопросов модели в раннере `scope=agent`: добавлены распознавания формулировок `уточняющий`, `нужно уточнение`, `согласны ли`, `подскажите` и других вариантов. Это устраняет кейс, когда модель задавала уточнение, но раннер ошибочно завершал прогон как `success` без продолжения имитации диалога. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.18.1 → v1.18.2 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**20.03.2026 >** Багфикс: ошибка прогона №21 — увеличен таймаут шага агента и улучшена диагностика timeout

**2.1 В приложении Комиссионка (web)**  
В `scope=agent` раннере увеличен дефолтный таймаут запроса к агенту с 60s до 180s (`AGENT_FETCH_TIMEOUT_MS`), чтобы длинные ответы модели не падали преждевременно. Также добавлена явная диагностика timeout (`agentTimeout`, `timeoutMs`) в `comparisonResult`/`diagnostics`, чтобы причина ошибок прогона была читаемой в карточке. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.18.2 → v1.18.3 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**20.03.2026 >** Фича: имитация пользователя до совпадения результата агента с expectedText

**2.1 В приложении Комиссионка (web)**  
Для `scope=agent` тест‑раннер теперь поддерживает рекурсивный диалог: он продолжает сценарий до тех пор, пока ожидаемый результат (`parameters.expectedText`) не будет достигнут, даже если модель не задаёт явные уточняющие вопросы. На каждом ходе имитируется ответ пользователя (с вариативностью), чтобы довести агент до финального корректного действия. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.17.1 → v1.17.2.
Заменено строк core: 18 из 13849. 

**20.03.2026 >** Багфикс: продолжать диалог до идентификации, даже если БД уже в нужном состоянии

**2.1 В приложении Комиссионка (web)**  
Для `scope=agent` тест‑раннера добавлено условие: если ожидаемый результат уже достигнут (например, `items` удалён или expectedText найден), но модель при этом всё ещё просит уточнение, прогон продолжает имитацию ответов пользователя до момента, когда уточнение перестанет требоваться. Это устраняет ситуацию, когда разговор в `conversationLog` обрывался после первого ответа модели. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.17.2 → v1.17.3.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**20.03.2026 >** Багфикс: добавить таймаут запросов к агенту, чтобы тест-прогоны не зависали на `running`

**2.1 В приложении Комиссионка (web)**  
В исполнителе `POST /api/admin/test-cases/[id]/run` для `scope=agent` добавлен `AbortController` таймаут на запросы к агенту (по умолчанию 60s через `AGENT_FETCH_TIMEOUT_MS`). При таймауте прогон завершается с `failed/error` и диагностикой, вместо вечного `running`. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.17.3 → v1.17.4 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**19.03.2026 >** Фича: рекурсивная имитация ответов пользователя при диалогах агента в тестах + сохранение `conversationLog`

**2.1 В приложении Комиссионка (web)**  
Для `scope=agent` добавлена поддержка диалогового сценария: если агент в `result` задаёт уточняющий вопрос, тест‑раннер имитирует ответы пользователя (может использовать `parameters.userResponses` или детерминированные варианты) и рекурсивно продолжает прогон до завершения. Одновременно сохраняется читаемая история чата в `test_runs.conversation_log`, которая доступна в карточке деталей прогона. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts), [src/components/AdminTestCatalogTab.tsx](src/components/AdminTestCatalogTab.tsx). Заменено строк core: 281 из 13837. app v1.16.14 → v1.17.0 (минор).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**19.03.2026 >** Фича: удобочитаемый рендер истории чата в карточке тест‑прогона

**2.1 В приложении Комиссионка (web)**  
В карточке деталей прогона в админке история диалога с агентом (`conversationLog`) теперь отображается как текстовая переписка (роль + сообщение), а не сырой JSON. Файл: [src/components/AdminTestCatalogTab.tsx](src/components/AdminTestCatalogTab.tsx). app v1.17.0 → v1.17.1 (патч).
Заменено строк core: 1 из 13837.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**19.03.2026 >** Багфикс: для UUID expectedText fallback-удаление через Prisma

**2.1 В приложении Комиссионка (web)**  
Для `scope=agent` тест‑кейса, где `parameters.expectedText` — UUID `items`, после ретрая и/или если агент не выполнил tool_calls, тест‑раннер теперь проверяет БД и при необходимости выполняет fallback‑удаление `items` через Prisma. Успех `dbItemDeletedById` определяется по факту отсутствия записи в БД. Это устраняет провалы, когда агент в `consult` не имеет доступа к tool_calls и возвращает “операция невозможна…”. Заменено строк core: 16 из 13558. app v1.16.12 → v1.16.13 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**19.03.2026 >** Багфикс: ретрай UUID-удаления в `consult`, когда агент в `dev` просит уточнение

**2.1 В приложении Комиссионка (web)**  
Для `scope=agent` если `parameters.expectedText` выглядит как UUID товара и первый ответ не приводит к удалению, добавлен ретрай: повторный вызов агента делается в режиме `consult` и с явным запретом задавать уточняющие вопросы. Это повышает устойчивость сценария “уточнение → затем действие” для тест‑кейса удаления по id. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.16.11 → v1.16.12 (патч).
Заменено строк core: 5 из 13546. app v1.16.11 → v1.16.12 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

### ______***UPDATE***________

**19.03.2026 >** Багфикс: если агент вернул ошибку consult-mode, тест не должен считаться success

**2.1 В приложении Комиссионка (web)**  
Для `scope=agent` тест‑раннер теперь не устанавливает `success`, если ответ агента содержит “операция невозможна… режим консультации”. Также ретрай UUID‑удаления делается в том же режиме, что указан в `parameters.mode` (без принудительного перехода в `consult`), чтобы не нарушать ожидания тест‑кейса. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.16.13 → v1.16.14 (патч).
Заменено строк core: 24 из 13576.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**19.03.2026 >** Багфикс: expectedText(UUID) для scope=agent теперь проверяется в БД

**2.1 В приложении Комиссионка (web)**  
В `POST /api/admin/test-cases/[id]/run` для `scope=agent` если `parameters.expectedText` выглядит как UUID, то успех теста определяется по факту отсутствия записи `items` в БД (через Prisma), а не по наличию UUID в тексте/steps агента. Это позволяет засчитывать сценарии “агент сначала уточнил, затем удалил”, даже если UUID не попал в финальный ответ. Заменено строк core: 27 из 13517. app v1.16.9 → v1.16.10 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**19.03.2026 >** Багфикс: ретрай для удалений по UUID, если агент уточняет намерение

**2.1 В приложении Комиссионка (web)**  
Для `scope=agent` если `parameters.expectedText` выглядит как UUID товара и после первого ответа запись `items` ещё существует — делается один ретрай в агенте с явной командой “удалите item.id=<UUID>”. Успех снова определяется по факту удаления записи из БД. Заменено строк core: 31 из 13542. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.16.10 → v1.16.11 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**19.03.2026 >** Багфикс: исполнитель тест‑кейсов маппит `mode/model` (\"Разработка\"/\"Gemini 3 Pro\") на `dev` и корректный model id

**2.1 В приложении Комиссионка (web)**  
Исправлено выполнение тест‑кейса №1: перед отправкой в агент `POST /api/admin/test-cases/[id]/run` теперь нормализует значения из `parameters` (например \"Разработка\" → `dev`, \"Gemini 3 Pro\" → `gemini-3-pro-preview`), чтобы агент получил валидные значения режима и модели. Диагностика усилена: при `error` от агента статус ставится `failed` и сохраняется `agentError` в `diagnostics`. Заменено строк core: 44 из 13470. app v1.16.1 → v1.16.2 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**19.03.2026 >** Багфикс: кнопка «Обогатить спецификацию…» теперь сохраняет `parameters` (model/mode/userPrompt/expectedText)

**2.1 В приложении Комиссионка (web)**  
В карточке тест‑кейса во вкладке **«Тесты»** исправлено обогащение спецификации: при нажатии **«Обогатить спецификацию тест‑кейса с ИИ»** ИИ возвращает JSON с `parameters`, а UI сохраняет `parameters` в `test_cases` через `PUT /api/admin/test-cases`. Это нужно, потому что исполнитель для `scope=agent` берёт `userPrompt/expectedText` именно из `test_cases.parameters`. Файл: [src/components/AdminTestCatalogTab.tsx](src/components/AdminTestCatalogTab.tsx). app v1.16.4 → v1.16.5 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**19.03.2026 >** Багфикс: при пустых параметрах тест‑кейса №1 показывать явную диагностику

**2.1 В приложении Комиссионка (web)**  
В `POST /api/admin/test-cases/[id]/run` для `scope=agent` добавлена проверка: если в `test_cases.parameters` отсутствует `expectedText` (или он пустой), проставляется `failed` с check `expectedTextMissing` и понятной `diagnostics`. Это предотвращает «немые» провалы (checks=[]), когда ожидаемый текст не был заполнен, и помогает использовать «Обогатить спецификацию…». Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). app v1.16.3 → v1.16.4 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**19.03.2026 >** Багфикс: заблокирован запуск `scope=agent` при пустом `parameters.expectedText`

**2.1 В приложении Комиссионка (web)**  
В карточке тест‑кейса во вкладке **«Тесты»** и в таблице блокируется кнопка запуска для `scope=agent`, если в `test_cases.parameters` отсутствует/пустой `expectedText`. Добавлено предупреждение с подсказкой сначала нажать «Обогатить спецификацию тест‑кейса с ИИ». Это предотвращает «немые» провалы прогонов (expectedTextMissing). Файл: [src/components/AdminTestCatalogTab.tsx](src/components/AdminTestCatalogTab.tsx). app v1.16.5 → v1.16.6 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**19.03.2026 >** Багфикс: для scope=agent expectedText ищется в steps/payload, а не только в финальном ответе

**2.1 В приложении Комиссионка (web)**  
В `POST /api/admin/test-cases/[id]/run` для `scope=agent` критерий `containsExpectedText` теперь ищет `parameters.expectedText` не только в `data.result`, но и в сериализованных `steps`/payload агента. Это позволяет засчитывать сценарии, где агент сначала задаёт уточнение, а затем выполняет действие (UUID может оказаться в tool-результатах, а не в финальном “вопросном” сообщении). Заменено строк core: 18 из 13500. app v1.16.8 → v1.16.9 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**19.03.2026 >** Багфикс: в карточке тест‑кейса добавлена кнопка «Обогатить спецификацию с ИИ»

**2.1 В приложении Комиссионка (web)**  
В диалоге карточки тест‑кейса во вкладке **«Тесты»** добавлена кнопка **«Обогатить спецификацию тест‑кейса с ИИ»**. По нажатию выполняется запрос к агенту (Gemini 3 Pro в режиме `consult`) с требованием вернуть строго JSON, после чего кейс обновляется через `PUT /api/admin/test-cases` (description, uiPages, apiEndpoints, codeRefs, dbEntities, promptTemplate/expectedResult при наличии). Файл: [src/components/AdminTestCatalogTab.tsx](src/components/AdminTestCatalogTab.tsx). app v1.16.2 → v1.16.3 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**05.03.2026 >** Фича: вкладка «Тесты» в админке (каталог тест‑кейсов, карточка кейса и прогона, запуск из UI)

**2.1 В приложении Комиссионка (web)**  
Добавлена вкладка **«Тесты»** (`#tests`) в [src/app/admin/page.tsx](src/app/admin/page.tsx), компонент [src/components/AdminTestCatalogTab.tsx](src/components/AdminTestCatalogTab.tsx): таблица кейсов с числом прогонов, статусом последнего прогона и % успешности; диалог карточки кейса (описание, привязки к UI/API/коду/БД, JSON параметров, история прогонов, «Запустить сейчас»); диалог деталей прогона (сравнение, шаги, диагностика, ссылка на лог агента); форма создания кейса. API: [src/app/api/admin/test-modules/route.ts](src/app/api/admin/test-modules/route.ts) — справочник модулей; в `GET /api/admin/test-cases` добавлен расчёт `successRate` по всем прогонам. Миграции БД: [20260319140000_add_test_catalog_tables](prisma/migrations/20260319140000_add_test_catalog_tables/migration.sql) (создание таблиц), [20260319150000_seed_test_modules](prisma/migrations/20260319150000_seed_test_modules/migration.sql) (сид модулей). Документация: [docs/manual/test-catalog-architecture.md](docs/manual/test-catalog-architecture.md) (Этап 4 отмечен выполненным), [docs/manual/app-komissionka.md](docs/manual/app-komissionka.md). Заменено/добавлено в core web ~260 строк из 13438. app v1.16.0 → v1.16.1 (патч, завершение UI каталога).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**05.03.2026 >** Фича: универсальный каталог тест‑кейсов (модели БД и Admin API для списка и истории прогонов)

**2.1 В приложении Комиссионка (web)**  
Добавлены Prisma‑модели `test_modules`, `test_cases`, `test_runs` для универсального каталога тестирования всех модулей (web, агент, Telegram‑бот, PWA, deploy‑воркер и т.д.): сценарии тест‑кейсов с параметрами (включая ПАРАМЕТР 1–4 для ИИ‑сценариев), ссылками на страницы/эндпоинты/файлы и историей прогонов с артефактами (reasoning‑log, дампы, диагностика). Реализован базовый Admin API: `GET/POST/PUT/DELETE /api/admin/test-cases` (каталог кейсов), `GET /api/admin/test-cases/runs/[testCaseId]` (список прогонов по кейсу), `GET /api/admin/test-cases/runs/detail/[runId]` (детали прогона). Документация обновлена: `docs/manual/test-catalog-architecture.md`, `docs/manual/app-komissionka.md`. Заменено/добавлено ~380 строк в core web (из 13177 строк). app v1.15.5 → v1.16.0 (минор, новая фича).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**05.03.2026 >** Багфикс: лог рассуждений («Ход выполнения») всегда актуален к текущему запросу, не залипает на прошлом ответе при ошибке

**2.1 В приложении Комиссионка (web)**  
При отправке сообщения в чат ИИ сразу выставляется шаг с текущим промптом и длиной; при ошибке HTTP/JSON/таймауте панель получает шаги этой ошибки (или `steps` из ответа API), `logId` сбрасывается или берётся из ответа. Маршрут `POST /api/admin/agent/run` для всех ошибок (403/400/502, ответ агента не 200, сбой соединения) возвращает JSON с полем `steps`, чтобы клиент не показывал устаревший лог. Файлы: [src/app/admin/page.tsx](src/app/admin/page.tsx), [src/app/api/admin/agent/run/route.ts](src/app/api/admin/agent/run/route.ts). Заменено/добавлено по сути в этих файлах (core web всего 12797 строк). app v1.15.4 → v1.15.5 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**19.03.2026 >** Багфикс: `run_command` теперь разрешает `npx tsx scripts/*.ts` с аргументами (нужно для удаления/утилит)

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.15.4.

**2.2 В сервисе Агент к модели ИИ**  
Исправлен ваш кейс: `npx tsx scripts/delete-item.ts <id>` был заблокирован whitelist’ом (разрешалось только без аргументов). Теперь `run_command` разрешает `npx tsx scripts/<имя>.ts [args...]` (инъекции по‑прежнему режутся `DANGEROUS_CHARS` и `isDangerousCommand`). Заменено строк core: `agent/tools/run-command.ts` — 3+2 из 3954 строк. agent v1.7.7 → v1.7.8 (патч). Файл: [agent/tools/run-command.ts](agent/tools/run-command.ts).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**19.03.2026 >** Багфикс: DEV guard больше не ломает поток, если модель уже задала вопросы и одновременно вернула tool_calls

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.15.4.

**2.2 В сервисе Агент к модели ИИ**  
Исправлен ваш кейс из дампа `status-20260319-104638.dump`: модель задала уточняющие вопросы и параллельно вернула `tool_calls: [read_file]`, после чего guard блокировал инструменты и получалась “ошибка/разрыв”. Теперь в `dev` при отсутствии вопросов в истории, но наличии уточняющих вопросов в *текущем* `response.content`, агент **не выполняет tool_calls**, а возвращает только текст вопросов/плана и ждёт ответа пользователя. Заменено строк core: agent core — 12+5 из 3953 строк. agent v1.7.6 → v1.7.7 (патч). Файл: [agent/core.ts](agent/core.ts).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**18.03.2026 >** Багфикс: вопросы уточнения в режиме dev теперь релевантны намерению (например удаление карточек товара)

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.15.4.

**2.2 В сервисе Агент к модели ИИ**  
В `agent/core.ts` уточняющие вопросы для RADICAL DEV guard теперь генерируются по намерению из `user prompt` (например для «удали две карточки товара» агент спрашивает про какие именно карточки/как выбрать их), чтобы агент не задавал нерелевантные вопросы и не “уезжал” в другую ветку выполнения. Заменено строк core: agent core — 33 из 3946 строк. agent v1.7.5 → v1.7.6 (патч). Файл: [agent/core.ts](agent/core.ts).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**18.03.2026 >** Багфикс/радикальное исправление: в режиме `dev` запрещены tool-вызовы до появления уточняющего вопроса от модели

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.15.4.

**2.2 В сервисе Агент к модели ИИ**  
В режиме `dev` добавлен радикальный guard в `agent/core.ts`: любые tool-вызовы блокируются, пока в истории ассистента нет хотя бы одного вопросительного уточнения. Это устраняет повторяющийся сценарий, когда модель «молча» выполняла tool_calls (например `sed/run_command`) и не задавала вопросы при получении задания. Замена: agent core — 30 из 3919 строк. agent v1.7.4 → v1.7.5. Файл: [agent/core.ts](agent/core.ts).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**18.03.2026 >** Багфикс: запросы с `!` больше не зависят от «памяти модели» (системный промпт принудительно пересылается)

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.15.4.

**2.2 В сервисе Агент к модели ИИ**  
Для `force-fresh` запросов (промпт начинается с `!`) добавлена гарантия пересылки системного промпта в модель. Это закрывает кейс из дампов, где системный промпт не отправлялся (`0 симв.`) и модель снова пыталась использовать запрещённые команды (например `sed`) вместо `read_file offset_lines/limit_lines`. agent v1.7.3 → v1.7.4 (патч). Файлы: [agent/contract.ts](agent/contract.ts), [agent/core.ts](agent/core.ts).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**18.03.2026 >** Багфикс: системный промпт снова гарантированно пересылается после деплоя (даже если последние ответы без подвала)

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.15.4.

**2.2 В сервисе Агент к модели ИИ**  
Исправлена логика «системный промпт не отправляется»: ранее сравнение версии брало только *последний* ответ ассистента, но при цепочке ошибок последними сообщениями часто оказываются короткие ответы без строки `agent vX.Y.Z`. Теперь версия определяется по последнему найденному упоминанию `agent vX.Y.Z` во всей истории ассистента, и при несовпадении с текущей версией промпт пересылается заново (контекст модели обновляется, включая правила про `read_file offset_lines/limit_lines` вместо `sed`). agent v1.7.2 → v1.7.3 (патч). Файл: [agent/core.ts](agent/core.ts).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**18.03.2026 >** Багфикс/улучшение агента: чтение больших файлов без sed + актуальный системный промпт после деплоя

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.15.4.

**2.2 В сервисе Агент к модели ИИ**  
`read_file` научился читать фрагменты больших файлов через параметры `offset_lines/limit_lines`, чтобы агент не пытался использовать запрещённые `sed/head/tail` для просмотра кусочков. Также в `agent/core.ts` добавлено правило: если в истории видна другая версия `agent vX.Y.Z`, системный промпт отправляется заново — это устраняет ситуацию «системный промпт не отправляется» после деплоя и возвращает ожидаемое поведение (уточняющие вопросы, политика прав, правила логов). agent v1.7.1 → v1.7.2 (патч). Файлы: [agent/tools/read-file.ts](agent/tools/read-file.ts), [agent/tools/index.ts](agent/tools/index.ts), [agent/core.ts](agent/core.ts), [agent/llm/system-prompt.ts](agent/llm/system-prompt.ts).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

### ______***UPDATE***________

**17.03.2026 >** Багфикс: «Не удалось отправить запрос к агенту» и ложные ошибки в панели ходов

**2.1 В приложении Комиссионка (web)**  
Для чата «Комиссионка AI» отключён SSE‑стриминг в запросе к `/api/admin/agent/run` (он периодически падал в Next.js с `failed to pipe response`/`ECONNRESET` и приводил к сообщению «Не удалось отправить запрос к агенту»). Теперь используется надёжный режим: один JSON‑ответ с `result/steps/logId`, при этом панель «Ход выполнения» и ссылка на лог рассуждений сохраняются (появляются после завершения запроса). app v1.15.3 → v1.15.4 (патч). [src/app/admin/page.tsx](src/app/admin/page.tsx).

**2.2 В сервисе Агент к модели ИИ**  
Исправлена ложная пометка tool‑ошибок: `read_file/grep` больше не считаются ошибкой из‑за слова `Error` внутри содержимого файла; ошибка определяется только по префиксу результата инструмента (`[tool] error:` / `[error]`). agent v1.7.0 → v1.7.1 (патч). [agent/core.ts](agent/core.ts).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

### ______***UPDATE***________

**17.03.2026 >** Этап 6–7: фильтр некритичных ошибок в панели ходов и уточняющие вопросы перед изменениями

**2.1 В приложении Комиссионка (web)**  
В панели «Ход выполнения» на вкладке «Комиссионка AI» теперь красная метка «ошибка» показывается только для действительно проблемных шагов инструментов (где в резюме результата явно есть error/ошибка), а не для любых вспомогательных вызовов. Это убирает визуальный шум при многократных read_file/grep и позволяет администратору быстрее видеть только критичные сбои. [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.15.2 → v1.15.3 (патч, UX‑улучшение панели ходов).

**2.2 В сервисе Агент к модели ИИ**  
В системном промпте режима «разработка» ужесточён блок «УТОЧНЕНИЕ И ПЛАН»: агент обязан почти всегда сначала задать хотя бы один уточняющий вопрос (кроме тривиальных запросов), выяснить, какие файлы/модули можно трогать и как будет проверяться результат, и только после этого строить план и вызывать write_file/run_command. Это снижает риск «молчаливых» правок без уточнений и делает поведение ближе к ожидаемому режиму Cursor. [agent/llm/system-prompt.ts](agent/llm/system-prompt.ts). agent v1.6.2 → v1.7.0 (минор, поведенческое изменение).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

### ______***UPDATE***________

**17.03.2026 >** Этапы 3–4: политика прав в промпте и run_command; удалена логика pending approval в агенте

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.15.1.

**2.2 В сервисе Агент к модели ИИ**  
По [docs/design-agent-no-confirmations.md](docs/design-agent-no-confirmations.md): (Этап 3) В системный промпт режима «разработка» добавлена секция ПОЛИТИКА ПРАВ: разрешённые каталоги, запрет на удаление/изменение пользователя bshevelev@mail.ru, deleteMany только с явным where, команды только из белого списка. В `agent/tools/run-command.ts` добавлена явная блокировка опасных шаблонов (rm -rf /, fork bomb, TRUNCATE/DROP DATABASE, пути в /etc, /var и т.д.) до проверки белого списка. (Этап 4) Удалена логика подтверждения вторым сообщением: в `agent/core.ts` убраны импорт и использование approval-store, блоки «откат»/getAndConsumePendingApproval и запрос подтверждения перед write_file/run_command — инструменты выполняются сразу в одном диалоге. В `agent/contract.ts` убраны isConfirmationCode и isShortConfirmation из forceFresh. Удалены неиспользуемые getCommandDescription/getWriteFileDescription из core. agent v1.6.0 → v1.6.1 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

### ______***UPDATE***________

**17.03.2026 >** Этап 5: UI админки под новый режим агента (без внешнего подтверждения)

**2.1 В приложении Комиссионка (web)**  
На вкладке «Комиссионка AI» в админке обновлён текстовый блок над чат‑окном: теперь он явно описывает новое поведение агента в стиле Cursor (уточняющие вопросы, план, затем пошаговое выполнение внутри одного диалога, без отдельного шага «да»/кода). Также в клиентском нормализаторе ответа `normalizeAgentResultForDisplay` удалена автодобавка подсказки «Отправьте второе сообщение…» — админка больше не показывает UX, завязанный на втором техническом сообщении. [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.15.1 → v1.15.2 (патч, UX‑синхронизация с новым агентом).

**2.2–2.4**  
Без изменений. agent v1.6.2, tgbot v1.1.0.

---

### ______***UPDATE***________

**17.03.2026 >** Этапы 1–2: агент без внешнего подтверждения («да»/код) — обновлён системный промпт и API

**2.1 В приложении Комиссионка (web)**  
Без изменений логики; нормализатор ответа агента уже не добавляет призыв «Отправьте второе сообщение» и убирает устаревшие фразы про 4 цифры/подтверждение кодом. app v1.15.1.

**2.2 В сервисе Агент к модели ИИ**  
В режиме «разработка» системный промпт переписан по [docs/design-agent-no-confirmations.md](docs/design-agent-no-confirmations.md): (1) УТОЧНЕНИЕ И ПЛАН — уточняющие вопросы, подробный план, диалог без глобального «да» и 4‑значных кодов; (2) ВЫПОЛНЕНИЕ ПЛАНА — выполнение в том же диалоге без ожидания второго технического сообщения; (4) явный пункт про перезапуск pm2; нумерация 1–6. agent v1.5.9 → v1.6.0 (минор, фича).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

### ______***UPDATE***________

**16.03.2026 >** Агент периодически падал с EADDRINUSE на порту 3140 при подтверждении «да»

**2.1 В приложении Комиссионка (web)**  
В API `/api/admin/agent/run` расширен список ошибок, при которых запрос к агенту повторяется автоматически: добавлен `EADDRINUSE` (как и ECONNRESET/ECONNREFUSED). При временных сбоях агента (перезапуск, краткий конфликт порта) админка ещё раз попробует получить ответ, прежде чем показывать ошибку. [src/app/api/admin/agent/run/route.ts](src/app/api/admin/agent/run/route.ts). app v1.15.1 (без изменений).

**2.2 В сервисе Агент к модели ИИ**  
В `agent/serve.ts` добавлен явный обработчик `server.on("error")`: при `EADDRINUSE` (порт 3140 уже занят) агент не входит в цикл перезапусков, а логирует понятное сообщение и завершает процесс, чтобы не пытаться слушать порт поверх другого экземпляра. Это упрощает диагностику и исключает повторяющиеся падения при запуске. [agent/serve.ts](agent/serve.ts). agent v1.5.8 → v1.5.9 (патч, багфикс).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

### ______***UPDATE***________

**10.03.2026 >** Диагностика агента в админке больше не засоряет чат техническими логами

**2.1 В приложении Комиссионка (web)**  
После сетевой ошибки при вызове агента админка по‑прежнему делает запрос `/api/admin/agent/diagnose`, но результат (pm2 list, curl /health, логи) больше не вставляется целиком в чат «Комиссионка AI». Вместо этого в чат добавляется только короткая подсказка с инструкцией нажать кнопку «ДАМП СОСТОЯНИЯ» и прислать путь к файлу дампа, а полный технический вывод доступен только через дамп‑файл. [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.15.0 → v1.15.1 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.8, tgbot v1.1.0.

---

### ______***UPDATE***________

**10.03.2026 >** Кнопка «ДАМП СОСТОЯНИЯ» в админке для быстрой диагностики ошибок и версий

**2.1 В приложении Комиссионка (web)**  
Добавлена кнопка «ДАМП СОСТОЯНИЯ» на вкладке «Комиссионка AI» в админке: по нажатию вызывается API `/api/admin/status-dump`, который запускает скрипт `scripts/status-dump.sh` на сервере. Скрипт собирает в файл `status-YYYYMMDD-HHMMSS.dump` системную информацию, версии, статус pm2-процессов (app/agent/bot/deploy-worker), занятые порты, curl /health и /api/version, последние логи, размеры ключевых директорий. Результат и путь к файлу отображаются прямо в админке; этот файл используется агентом для детальной диагностики при сообщении «ошибка». [src/app/api/admin/status-dump/route.ts](src/app/api/admin/status-dump/route.ts), [src/app/admin/page.tsx](src/app/admin/page.tsx), [scripts/status-dump.sh](scripts/status-dump.sh). app v1.14.7 → v1.15.0 (минор, фича).

**2.2–2.4**  
Без изменений. agent v1.5.8, tgbot v1.1.0.

---

### ______***UPDATE***________

**10.03.2026 >** Вкладка «Деплой» в админке: версия агента на prod не совпадала с подвалом отчётов (показывала 1.5.3 вместо 1.5.8)

**2.1 В приложении Комиссионка (web)**  
API `/api/version` больше не читает версию агента через `/health` сервиса агента (где мог использоваться другой `version.json`, из-за чего в админке на вкладке «Деплой» отображалась устаревшая версия). Теперь все три значения (app/agent/tgbot) берутся только из корневого `version.json` через `src/lib/versions.ts`, поэтому версии в админке и в подвале отчётов агента совпадают. [src/app/api/version/route.ts](src/app/api/version/route.ts). app v1.14.6 → v1.14.7 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.8, tgbot v1.1.0.

---

**08.03.2026 >** «да» всё ещё из кэша — в dev для любого короткого ответа (≤20 символов) отключён кэш

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.14.6.

**2.2 В сервисе Агент к модели ИИ**  
В режиме разработка для любого промпта длиной ≤20 символов включён forceFresh (кэш не используется), чтобы ответы «да», «ок» и т.п. всегда доходили до агента и выполнялся план подтверждения. Раньше проверялся только список слов; теперь достаточно короткой длины. [agent/contract.ts](agent/contract.ts). agent v1.5.7 → v1.5.8 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

**08.03.2026 >** Деплой prod: повторный Exit 134 при npm install

**2.1 В приложении Комиссионка (web)**  
В [scripts/env-deploy.sh](scripts/env-deploy.sh): лимит памяти снижен до 1536 MB (чтобы несколько процессов Node помещались в RAM); npm ci с `--prefer-offline --no-audit`; перед fallback на npm install — очистка кэша (`npm cache clean --force`) и повторное `rm -rf node_modules`; npm install с `--no-audit --no-fund --prefer-offline`. Для сборки явно задан тот же NODE_OPTIONS. app v1.14.5 → v1.14.6 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.7, tgbot v1.1.0.

---

**08.03.2026 >** «да» после плана попадало в кэш — план не выполнялся, снова показывался запрос подтверждения

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.14.4.

**2.2 В сервисе Агент к модели ИИ**  
Короткие ответы «да», «давай», «ок», «подтверждаю» и т.п. в режиме разработка больше не берутся из кэша (forceFresh), чтобы агент реально вызывался и обрабатывал getAndConsumePendingApproval() — выполнял план. Иначе «да» совпадало с кэшем из другой сессии (100%) и возвращался старый ответ вместо выполнения. Для «откат» (без кода) также принудительно без кэша. [agent/contract.ts](agent/contract.ts). agent v1.5.6 → v1.5.7 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

**07.03.2026 >** Деплой только через очередь; прямой SSH и -NoQueue отключены

**2.1 В приложении Комиссионка (web)**  
Скрипт [scripts/deploy-hoster-git.ps1](scripts/deploy-hoster-git.ps1): убран параметр `-NoQueue` и fallback на прямой SSH при сбое API. Деплой возможен только путём постановки задачи в очередь (POST /api/deploy/queue); при недоступности API скрипт завершается с ошибкой. Скрипт передаёт заголовок `X-Agent-API-Key` из `$env:AGENT_API_KEY` для авторизации; при 403 выводится подсказка (установить ключ или запускать деплой из админки). Резервный путь — только `deploy-hoster.ps1 -Upload -Restart` (scp). Обновлены [AGENTS.md](AGENTS.md), [docs/manual/DEPLOY-AGENT.md](docs/manual/DEPLOY-AGENT.md), [docs/manual/app-komissionka.md](docs/manual/app-komissionka.md), правила .cursor. app v1.14.3 → v1.14.4 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.6, tgbot v1.1.0.

---

**07.03.2026 >** Прод-среда: не показывала версию и статус active (показывала stopped)

**2.1 В приложении Комиссионка (web)**  
Вкладка Деплой: для каждой среды версия запрашивается с порта приложения независимо от поля status в БД; если приложение отвечает на port_app — в ответе подставляются status «active» и версии (app/agent/tgbot). Прод отображается как активный с версией, если приложение реально запущено. [src/app/api/deploy/environments/route.ts](src/app/api/deploy/environments/route.ts). app v1.14.2 → v1.14.3 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.6, tgbot v1.1.0.

---

**07.03.2026 >** Деплой prod: failed — в отчёте не было причины; улучшение скриптов деплоя

**2.1 В приложении Комиссионка (web)**  
При следующем сбое деплоя в журнал и Telegram-уведомление попадёт конкретная причина: код выхода и команда (ERR trap в скриптах). [scripts/env-deploy.sh](scripts/env-deploy.sh), [scripts/deploy-from-git.sh](scripts/deploy-from-git.sh). Для prod в env-deploy.sh добавлен перезапуск deploy-worker (как в deploy-from-git.sh). app v1.14.1 → v1.14.2 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.6, tgbot v1.1.0.

---

**07.03.2026 >** Подвал чата с моделью ИИ: версии не совпадали с админкой

**2.1 В приложении Комиссионка (web)**  
При вызове агента из админки приложение передаёт в теле запроса `footerVersions: { app, agent, tgbot }` из [src/lib/versions.ts](src/lib/versions.ts). Агент использует эти версии в подвале ответа, поэтому в чате отображаются те же версии, что и в интерфейсе. [src/app/api/admin/agent/run/route.ts](src/app/api/admin/agent/run/route.ts). app v1.14.0 → v1.14.1 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Добавлена поддержка опционального параметра `footerVersions` в запросе: при наличии используется в подвале отчёта вместо чтения version.json с диска. [agent/core.ts](agent/core.ts), [agent/contract.ts](agent/contract.ts), [agent/serve.ts](agent/serve.ts). agent v1.5.5 → v1.5.6 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

**07.03.2026 >** Уведомление администратору в Telegram при завершении деплоя

**2.1 В приложении Комиссионка (web)**  
После записи в журнал деплоя (POST /api/deploy/log/append) отправляется уведомление в Telegram всем администраторам (profiles с role=admin и заполненным telegram_id): статус (completed/failed), среда, операция, версии app/agent/tgbot, кто запустил, длительность, краткий вывод/ошибка. Реализовано: [src/lib/deploy-telegram-notify.ts](src/lib/deploy-telegram-notify.ts), вызов из [src/app/api/deploy/log/append/route.ts](src/app/api/deploy/log/append/route.ts). Токен бота — TELEGRAM_BOT_TOKEN. app v1.13.7 → v1.14.0 (минор, фича).

**2.2–2.4**  
Без изменений. agent v1.5.5, tgbot v1.1.0.

---

**07.03.2026 >** Подмена устаревшего «4 цифры» / «Подтвердите кодом» на инструкцию вторым сообщением

**2.1 В приложении Комиссионка (web)**  
Даже при старом агенте на проде пользователь не видит просьбу ввести 4 цифры: ответ агента нормализуется в API [src/app/api/admin/agent/run/route.ts](src/app/api/admin/agent/run/route.ts) и при отображении в чате [src/app/admin/page.tsx](src/app/admin/page.tsx). Удаляются «Подтвердите кодом: XXXX», «Повторите 4 цифры ответным сообщением»; при блоке подтверждения подставляется «Отправьте второе сообщение («да» или любой текст)… Для отката … «откат».» app v1.14.0 → v1.14.1 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.5, tgbot v1.1.0.

---

**07.03.2026 >** Ход рассуждений показывал лог не от текущего промпта

**2.1 В приложении Комиссионка (web)**  
При запросе лога по конкретному logId API мог отдавать last-reasoning.txt (последний записанный ход), если файл \<logId\>.log не находился — из-за этого открывался лог от другого запроса. Убран fallback на last-reasoning: теперь возвращается только файл по запрошенному logId; при отсутствии файла — 404 с сообщением «Лог для этого запроса не найден». [src/app/api/admin/agent/log/route.ts](src/app/api/admin/agent/log/route.ts). app v1.13.6 → v1.13.7 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.5, tgbot v1.1.0.

---

**07.03.2026 >** Вкладка Деплой: не видно активности; агент в другом чате не понимает, как деплоить

**2.1 В приложении Комиссионка (web)**  
Вкладка «Деплой»: запросы к API выполняются с `credentials: "include"`; при 403 показывается сообщение «Нет доступа к API деплоя. Войдите в админку как администратор»; под заголовком выводится строка «Обновлено: … Очередь: N, журнал: M записей» для отображения активности. [src/components/DeployTab.tsx](src/components/DeployTab.tsx). Добавлена инструкция для агента в другом чате: [docs/manual/DEPLOY-AGENT.md](docs/manual/DEPLOY-AGENT.md); в [AGENTS.md](AGENTS.md) — блок «Как деплоить (для агента в другом чате)» со ссылкой на эту инструкцию и пояснением про вкладку Деплой (очередь пуста после выполнения операций, активность в журнале). app v1.13.5 → v1.13.6 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.5, tgbot v1.1.0.

---

**07.03.2026 >** Режим «Разработка»: модель всё ещё просила 4 цифры; деплой не обновлял сервер

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.13.5.

**2.2 В сервисе Агент к модели ИИ**  
В системный промпт (режим разработка) добавлен явный запрет: «ЗАПРЕЩЕНО просить ввести код из 4 цифр или показывать «Подтвердите кодом: XXXX» — подтверждение только вторым сообщением». [agent/llm/system-prompt.ts](agent/llm/system-prompt.ts). В [agent/approval-store.ts](agent/approval-store.ts) обновлён комментарий (подтверждение вторым сообщением, не 4 цифры). Деплой выполнять с `-NoQueue` при сбоях очереди. agent v1.5.4 → v1.5.5 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

**07.03.2026 >** Лог рассуждений: «Log not found» — исправлено

**2.1 В приложении Комиссионка (web)**  
API [src/app/api/admin/agent/log/route.ts](src/app/api/admin/agent/log/route.ts): корень для чтения логов берётся из `PROJECT_ROOT` или `AGENT_ROOT` (как у агента), иначе `process.cwd()`. Если файл по `logId` не найден — отдаётся содержимое `last-reasoning.txt` (последний ход) с поясняющей строкой, чтобы не показывать пустой «Log not found». app v1.13.4 → v1.13.5 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.4, tgbot v1.1.0.

---

**07.03.2026 >** Деплой по умолчанию через очередь (worker); правки на проде после обработки очереди

**2.1 В приложении Комиссионка (web)**  
Деплой выполняется **через очередь и deploy-worker**: [scripts/deploy-hoster-git.ps1](scripts/deploy-hoster-git.ps1) по умолчанию отправляет задачу в `/api/deploy/queue`; worker на сервере выполняет `scripts/env-deploy.sh <среда> <ветка>`. Правки на проде появляются после обработки очереди (1–5 мин). Для прямого SSH без очереди добавлен ключ `-NoQueue`. Документация: [docs/manual/app-komissionka.md](docs/manual/app-komissionka.md) §7.1 — схема с worker, этапы. app v1.13.3 → v1.13.4 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.4, tgbot v1.1.0.

---

**07.03.2026 >** Отмена подтверждения четырьмя цифрами в режиме «Разработка»: достаточно второго сообщения

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.13.3.

**2.2 В сервисе Агент к модели ИИ**  
Убран функционал подтверждения 4-значным кодом. Подтверждение плана — вторым сообщением (любой текст или «да»). Откат — командой «откат» без кода. Изменения: [agent/core.ts](agent/core.ts), [agent/approval-store.ts](agent/approval-store.ts), [agent/llm/system-prompt.ts](agent/llm/system-prompt.ts). agent v1.5.3 → v1.5.4 (минор).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

**07.03.2026 >** Чаты с ИИ не сохранялись; EADDRINUSE на сервере — скрипт однократного исправления

**2.1 В приложении Комиссионка (web)**  
- **Сохранение чатов по режимам:** в [src/lib/ai-chats-storage.ts](src/lib/ai-chats-storage.ts) хранилище переведено на отдельные ключи по режиму (`chats_consult`, `chats_dev`, `chats_chat`). В каждом режиме своя серия чатов, данные не перезатираются при переключении режима; хранятся до удаления пользователем. Миграция со старого одного ключа выполняется при первой загрузке. В админке при загрузке передаётся текущий режим в `loadAiChats(aiMode)`.
- **Скрипт для сервера:** [scripts/fix-agent-port-on-server.sh](scripts/fix-agent-port-on-server.sh) — однократный запуск на сервере (`bash scripts/fix-agent-port-on-server.sh`) выставляет AGENT_PORT=3141 для test1 и перезапускает agent-test1 и agent, чтобы устранить EADDRINUSE :::3140. app v1.13.2 → v1.13.3 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.3, tgbot v1.1.0.

---

**07.03.2026 >** Исправление EADDRINUSE (порт 3140): тестовые среды не должны занимать порт prod-агента

**2.1 В приложении Комиссионка (web)**  
В [scripts/env-create.sh](scripts/env-create.sh): для новых тестовых сред порт агента больше не может совпадать с 3140 (если PORT_APP=3040, то PORT_AGENT сдвигается на 3141), чтобы избежать конфликта с prod-агентом и ошибки «listen EADDRINUSE :::3140». В ecosystem тестовой среды для процесса agent явно передаётся AGENT_PORT (вместо PORT). **Уже существующая среда test1:** если она создана с портом приложения 3040, на сервере в `~/komissionka-test1/.env` задать `AGENT_PORT=3141`, удалить или заменить старую строку AGENT_PORT=3140, затем `pm2 restart agent-test1`. app v1.13.1 → v1.13.2 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.3, tgbot v1.1.0.

---

**07.03.2026 >** Убрана ссылка «Подключить Claude» на экране чата с ИИ (админка)

**2.1 В приложении Комиссионка (web)**  
В блоке выбора модели ИИ на вкладке «Комиссионка AI» удалена ссылка «Подключить Claude» (на /admin/claude-setup). Ссылка «Справка по моделям» сохранена. [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.13.0 → v1.13.1 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.3, tgbot v1.1.0.

---

**06.03.2026 >** Сервис деплоя для управления множественными средами (prod, test1, test2...)

**2.1 В приложении Комиссионка (web)**  
Реализован полноценный сервис деплоя:
- **Модель данных Prisma**: 3 новые таблицы — `deploy_environments` (реестр сред), `deploy_queue` (очередь операций), `deploy_log` (журнал)
- **API endpoints** (8 шт): `/api/deploy/environments`, `/api/deploy/queue`, `/api/deploy/log`, `/api/deploy/copy`, `/api/deploy/status`
- **Bash-скрипты**: `env-create.sh`, `env-deploy.sh`, `env-copy.sh`, `env-delete.sh`, `env-status.sh`
- **Worker**: `deploy-worker.ts` — фоновый процесс обработки очереди (добавлен в PM2)
- **UI в админке**: новая вкладка «Деплой» с секциями Среды/Очередь/Журнал, диалоги создания/копирования сред
- **PowerShell**: обновлён `deploy-hoster-git.ps1` — параметр `-Env` для деплоя в конкретную среду

Новые файлы: `src/app/api/deploy/*`, `src/components/DeployTab.tsx`, `scripts/env-*.sh`, `scripts/deploy-worker.ts`.
Добавлено ~2200 строк из 12322 строк core app (~18%).
app v1.12.5 → v1.13.0 (минор — новая фича).

**2.2–2.4**  
Без изменений. agent v1.5.3, tgbot v1.1.0.

---

**06.03.2026 >** Исправление: «Duplicate function declaration found: backlog» при запросе к Gemini API в режиме консультации

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.12.5.

**2.2 В сервисе Агент к модели ИИ**  
Исправлен дубликат инструмента `backlog` в массиве `TOOLS_CONSULT`: `BACKLOG_TOOL` уже входит в `TOOLS_FOR_LLM`, поэтому явное добавление создавало дубль и ошибку 400 от Gemini API. Удалена лишняя строка. [agent/tools/index.ts](agent/tools/index.ts). agent v1.5.2 → v1.5.3 (патч).

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

**06.03.2026 >** Тикет №5: компактная таблица бэклога

**2.1 В приложении Комиссионка (web)**  
Таблица бэклога сделана компактнее: убраны колонки Модули, Компоненты, Документация, Создано, Изменено; шрифт уменьшен на ~10% (`text-[0.8rem]`); кнопки «Изменить»/«Удалить» заменены на иконки карандаша и корзины (lucide-react); «Краткое описание» теперь с переносом по словам (`break-words`). [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.12.4 → v1.12.5 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.2, tgbot v1.1.0.

---

**06.03.2026 >** Исправление: на главной странице отображалось только 10 товаров вместо всех

**2.1 В приложении Комиссионка (web)**  
Исправлен баг: на главной странице отображалось только 10 товаров из 29 (лимит в itemService.ts). Увеличен LATEST_ITEMS_LIMIT с 10 до 100 в [src/services/itemService.ts](src/services/itemService.ts). app v1.12.3 → v1.12.4 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.2, tgbot v1.1.0.

---

**06.03.2026 >** Тикет №5: исправление скролла после закрытия диалога и сохранение вкладки в URL

**2.1 В приложении Комиссионка (web)**  
Исправлены два бага: (1) После закрытия диалога редактирования тикета не работал скролл — Dialog рендерился условно и удалялся из DOM до cleanup Radix (overflow: hidden на body оставался). Теперь Dialog всегда рендерится, управляется только `open`. (2) При обновлении страницы терялась текущая вкладка — теперь активная вкладка сохраняется в URL hash (#backlog, #ai и т.д.) и восстанавливается при загрузке. [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.12.2 → v1.12.3 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.2, tgbot v1.1.0.

---

**06.03.2026 >** Тикет №2: WYSIWYG-редактор описания задачи (одно окно)

**2.1 В приложении Комиссионка (web)**  
Заменён Markdown-редактор с двумя панелями (@uiw/react-md-editor) на WYSIWYG-редактор Tiptap. Теперь одно окно с моментальным рендерингом: жирный, курсив, заголовки, списки, код — форматирование применяется сразу. Данные хранятся в Markdown (конвертация marked/turndown). Новый компонент [src/components/WysiwygEditor.tsx](src/components/WysiwygEditor.tsx), зависимости @tiptap/react, @tiptap/starter-kit, turndown, marked. app v1.12.1 → v1.12.2 (патч — улучшение UI по тикету №2).

**2.2–2.4**  
Без изменений. agent v1.5.2, tgbot v1.1.0.

---

**06.03.2026 >** Исправление: Select фильтров бэклога — пустое value

**2.1 В приложении Комиссионка (web)**  
Исправлена ошибка `A <Select.Item /> must have a value prop that is not an empty string`: в фильтрах «Статус задачи» и «Тип задачи» опция «Все» имела `value=""`, что вызывало краш страницы. Заменено на `value="__all__"`, логика `fetchBacklog` обновлена для игнорирования `__all__`. [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.12.0 → v1.12.1 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.2, tgbot v1.1.0.

---

**06.03.2026 >** Спринт 2, тикет №5: удобная навигация по таблице тикетов бэклога

**2.1 В приложении Комиссионка (web)**  
Реализована навигация по таблице бэклога (тикет №5). GET `/api/admin/backlog` с пагинацией (`_page`, `_limit`), сортировкой (`_sort`: order_num | short_description | created_at | task_status | id, `_order`: asc | desc) и фильтрами (`task_status`, `task_type`). В админке: панель сортировки/фильтров и кнопка «Обновить список»; под таблицей — пагинация «Страница N из M», кнопки «Предыдущая»/«Следующая». После сохранения и удаления тикета список обновляется через `fetchBacklog(backlogPage)`. В диалоге редактирования поле «Статус задачи» только для чтения (как в таблице). PATCH `/api/admin/backlog/[id]`: из UI принимаются только разрешённые поля (order_num, short_description, description_prompt, doc_link, test_order_or_link); sprint_number, task_status и др. — только от агента (заголовок X-Agent-API-Key). [src/app/api/admin/backlog/route.ts](src/app/api/admin/backlog/route.ts), [src/app/api/admin/backlog/[id]/route.ts](src/app/api/admin/backlog/[id]/route.ts), [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.11.0 → v1.12.0 (минор — тикет №5).

**2.2–2.4**  
Без изменений. agent v1.5.2, tgbot v1.1.0.

---

**06.03.2026 >** Спринт 2: сброс классификации при редактировании промпта, Markdown-редактор для описания тикета

**2.1 В приложении Комиссионка (web)**  
Реализованы две задачи спринта 2. (1) При ручном изменении поля «Описание/промпт» тикета (PATCH `/api/admin/backlog/[id]`) поля классификации ИИ сбрасываются: `task_type`, `modules`, `components`, `complexity` устанавливаются в `null`, чтобы при изменении сути задачи старая классификация не сохранялась. [src/app/api/admin/backlog/[id]/route.ts](src/app/api/admin/backlog/[id]/route.ts). (2) Поле описания/промпта в форме создания и в диалоге редактирования тикета заменено на Markdown-редактор (@uiw/react-md-editor): панель инструментов, live preview, сохранение в БД как строка Markdown. Компонент [src/components/MarkdownEditor.tsx](src/components/MarkdownEditor.tsx), интеграция в [src/app/admin/page.tsx](src/app/admin/page.tsx). Задача «вынести сущность спринта в справочник» оставлена на следующий этап. app v1.10.3 → v1.11.0 (минор — спринт 2).

**2.2–2.4**  
Без изменений. agent v1.5.2, tgbot v1.1.0.

---

**06.03.2026 >** Бэклог: поле «Об этом промпте» — новый формат и сворачиваемый блок в одну строку

**2.1 В приложении Комиссионка (web)**  
Содержимое поля «Об этом промпте» пересобрано: «Исходно задача звучала так: …», «Символов было в обмене с моделью ИИ: ввод X / вывод Y (слов: W1 / W2)», «Версии на момент генерации: app v…, agent v…, tgbot v…» (данные парсятся из подвала ответа агента или из version.json). Высота блока в админке — одна строка со стрелкой вниз (▼) для раскрытия; в развёрнутом виде до ~10 строк (max-height 15em) с вертикальным скроллом. [generate-prompt/route.ts](src/app/api/admin/backlog/[id]/generate-prompt/route.ts), [admin/page.tsx](src/app/admin/page.tsx). app v1.10.2 → v1.10.3 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.2, tgbot v1.1.0.

---

**06.03.2026 >** Бэклог: парсинг JSON при генерации промпта (тройные кавычки внутри prompt_markdown), мета-промпт про структуру

**2.1 В приложении Комиссионка (web)**  
Исправлена ошибка, при которой в поле описания тикета сохранялся обрубок промпта: регулярное выражение для извлечения блока ```json ... ``` останавливалось на первом вхождении ```, в том числе внутри строки prompt_markdown (например код ```prisma). Добавлена функция extractJsonBlockFromMarkdown в [src/app/api/admin/backlog/[id]/generate-prompt/route.ts](src/app/api/admin/backlog/[id]/generate-prompt/route.ts): закрывающие ``` ищутся с учётом того, что они могут быть внутри JSON-строки (учёт кавычек и экранирования). В мета-промпт генерации добавлено требование: промпт должен быть структурированным, понятным человеку, отражать суть задачи с минимально необходимой детализацией. app v1.10.1 → v1.10.2 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.2, tgbot v1.1.0.

---

**06.03.2026 >** Бэклог: в «Об этом промпте» — преамбула и подвал из ответа агента; в описании только суть задачи

**2.1 В приложении Комиссионка (web)**  
В [src/app/api/admin/backlog/[id]/generate-prompt/route.ts](src/app/api/admin/backlog/[id]/generate-prompt/route.ts) при генерации промпта из полного ответа агента извлекаются: преамбула (всё до блока ```json, в т.ч. «Gemini … 0> ```json») и подвал (блок «--- Модель: … Символов: … Версии: … Службы: …») — они сохраняются в поле «Об этом промпте» (prompt_about). В поле описания (description_prompt) сохраняется только суть задачи: содержимое prompt_markdown из JSON или очищенный от преамбулы/подвала текст. При отсутствии в ответе преамбулы/подвала в «Об этом промпте» по-прежнему подставляются Модель, Дата создания, Объём. app v1.10.0 → v1.10.1 (патч).

**2.2–2.4**  
Без изменений. agent v1.5.2, tgbot v1.1.0.

---

**06.03.2026 >** Бэклог: поле «Об этом промпте», чистый промпт, объём генерации, сохранение классификаторов и лога, системный промпт про тикет/спринт

**2.1 В приложении Комиссионка (web)**  
В модель backlog добавлено поле prompt_about (преамбула и подвал о промпте; не передаётся в Cursor/ИИ при исполнении). В поле description_prompt сохраняется только чистый промпт без преамбулы и подвала; преамбула (модель, дата, объём) выносится в «Об этом промпте» и отображается отдельно. Классификаторы (Тип задачи, Модули, Компоненты, Сложность) и prompt_log_id теперь возвращаются в GET /api/admin/data и сохраняются после генерации — ссылка на лог рассуждений не пропадает при повторном открытии тикета. Добавлен атрибут «Объём промпта» рядом с выбором модели: Кратко / Стандарт / Полная детализация. Мета-промпт генерации: убрана формулировка «про совпадение смыслов»; указано, что тестирование — в поле «Тестирование / ссылка на сценарии», не в общем описании. Миграция [prisma/migrations/20260306160000_backlog_prompt_about/migration.sql](prisma/migrations/20260306160000_backlog_prompt_about/migration.sql). app v1.9.4 → v1.10.0 (минор — бэклог).

**2.2 В сервисе Агент к модели ИИ**  
В системный промпт [agent/llm/system-prompt.ts](agent/llm/system-prompt.ts) добавлено правило: задание «выполни тикет №…» или «выполни спринт №…» подразумевает передачу в Cursor или модель ИИ только краткого и полного описания (short_description, description_prompt), без атрибутов «Об этом промпте», тип задачи, модули и т.д. agent v1.5.1 → v1.5.2 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений (0 из 860 строк core). tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

**06.03.2026 >** Бэклог: одна и та же генерация промпта для всех тикетов — отключение кэша и уникальный промпт

**2.1 В приложении Комиссионка (web)**  
Исправлена ошибка, при которой при редактировании любого тикета генерация промпта возвращала один и тот же текст. В [src/app/api/admin/agent/run/route.ts](src/app/api/admin/agent/run/route.ts) для запросов бэклога (chatName.startsWith("backlog:") или project === "Комиссионка backlog") принудительно передаётся disableCache: true агенту. В [src/app/api/admin/backlog/[id]/generate-prompt/route.ts](src/app/api/admin/backlog/%5Bid%5D/generate-prompt/route.ts): в начало мета-промпта добавлена уникальная строка «[Запрос {id}-{timestamp}]», вызов run выполняется с cache: "no-store", ответ — с заголовком Cache-Control: no-store. app v1.9.3 → v1.9.4 (патч).

**2.2 В сервисе Агент к модели ИИ**  
В [agent/contract.ts](agent/contract.ts) для запросов бэклога (chatName.startsWith("backlog:") или project === "Комиссионка backlog") принудительно отключается кэш (disableCache), чтобы не возвращать похожий ответ из agent_prompt_cache для разных тикетов. agent v1.5.0 → v1.5.1 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений (0 из 860 строк core). tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

**06.03.2026 >** Бэклог: строгое совпадение смыслов краткого описания и сгенерированного промпта (тикет №5)

**2.1 В приложении Комиссионка (web)**  
Усилен мета-промпт для генерации промпта ИИ по тикету: краткое описание объявлено единственным источником задачи, добавлен блок «КРИТИЧНО — совпадение смыслов» (prompt_markdown должен раскрывать только задачу из краткого описания, запрещено подставлять другую задачу или контекст). При вызове агента для генерации передаётся `history: []` и уникальный `chatName` с меткой времени, чтобы модель не опиралась на предыдущие реплики. Изменения в [src/app/admin/page.tsx](src/app/admin/page.tsx) (форма создания) и [src/app/api/admin/backlog/[id]/generate-prompt/route.ts](src/app/api/admin/backlog/%5Bid%5D/generate-prompt/route.ts) (окно редактирования). Кнопка «Сгенерировать промпт ИИ» на форме создания новой записи уже была; проверь обновление страницы (Ctrl+F5). app v1.9.2 → v1.9.3 (патч — совпадение смыслов описания и промпта).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений (0 из 3981 строк core). agent v1.5.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений (0 из 860 строк core). tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

**06.03.2026 >** Бэклог: мини‑выбор модели рядом с кнопкой генерации промпта, точное соответствие промпта тексту тикета

**2.1 В приложении Комиссионка (web)**  
Во вкладке «Бэклог» в админке рядом с кнопкой «Сгенерировать промпт ИИ» добавлен компактный Select выбора модели ИИ как в блоке «Комиссионка AI» (как в форме создания новой записи, так и в окне редактирования тикета). Выбор модели передаётся в `/api/admin/agent/run` через поле `model` и дальше — в агент, а префикс промпта теперь явно показывает, какая модель его создала. Это исправляет тикет №5 («улучшить окно редактирования описания тикета»): админ видит мини‑выбор модели прямо рядом с кнопкой генерации, а сгенерированный промпт однозначно относится к текущему тексту тикета, без влияния старых кэшированных ответов. Заменено 80 из 10710 строк core web (≈0.7%). app v1.9.1 → v1.9.2 (патч — UX/логика генерации промпта по тикету).

**2.2 В сервисе Агент к модели ИИ**  
Интерфейс HTTP‑роута админки `/api/admin/agent/run` научился принимать `model` в теле запроса и приоритетно использовать её как `modelOverride` для одного вызова агента, оставляя `site_settings.agent_llm_model` значением по умолчанию. Это позволяет бэклогу и другим частям админки выбирать модель для конкретной генерации, не меняя глобальные настройки. Изменения только в адаптере админки, core агента без изменений (0 из 3981 строк). agent v1.5.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений (0 из 860 строк core). tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

**06.03.2026 >** Бэклог: генерация промпта моделью ИИ по краткому описанию (тикет №1)

**2.1 В приложении Комиссионка (web)**  
Во вкладке «Бэклог» в админке добавлена генерация описания/промпта для ИИ по задаче бэклога (кнопка «Сгенерировать промпт ИИ» под полем описания в диалоге редактирования записи). Новый роут [src/app/api/admin/backlog/[id]/generate-prompt/route.ts](src/app/api/admin/backlog/%5Bid%5D/generate-prompt/route.ts) вызывает агента через `/api/admin/agent/run`, формирует полный Markdown-промпт на основе краткого описания и текущего текста, а также обновляет классификаторы задачи и метаданные промпта (тип задачи, модули, компоненты, сложность, модель, время генерации, logId). Модель бэклога расширена полями `task_type`, `modules`, `components`, `complexity`, `prompt_model`, `prompt_created_at`, `prompt_duration_sec`, `prompt_log_id` (миграция [prisma/migrations/20260306131000_backlog_prompt_fields/migration.sql](prisma/migrations/20260306131000_backlog_prompt_fields/migration.sql)), дубликат в [docs/backlog.md](docs/backlog.md) показывает эти поля. Практически это реализует тикет №1: «Добавить функцию создания промпта по краткому описанию задачи в бэклоге». Заменено 357 из 10603 строк core web (≈3.3%). app v1.8.2 → v1.9.0 (минор — новая функция бэклога).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений в core (0 из 3981 строк), но промпт для задачи бэклога теперь вызывается через существующий `/api/admin/agent/run`, а logId ответа используется для ссылки на лог рассуждений в админке («Лог рассуждений модели →»). agent v1.5.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений (0 из 860 строк core). tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

**06.03.2026 >** CLI для бэклога: scripts/backlog-cli.ts для Cursor и агента

**2.1 В приложении Комиссионка (web)**  
Добавлен CLI-скрипт [scripts/backlog-cli.ts](scripts/backlog-cli.ts) для управления таблицей backlog через Prisma: `list`, `get`, `create`, `update`, `delete`. Используется разработчиком и Cursor (через `npx tsx scripts/backlog-cli.ts ...`) для чтения и изменения бэклога без входа в UI. В документацию добавлена строка в [docs/manual/app-komissionka.md](docs/manual/app-komissionka.md) (раздел «Админка», блок «Бэклог»). Без изменений в core web: заменено 0 из 10260 строк. app v1.8.1 → v1.8.2 (патч — инфраструктура для бэклога).

**2.2 В сервисе Агент к модели ИИ**  
В описании разрешённых команд добавлена явная ссылка на `npx tsx scripts/backlog-cli.ts <cmd>` как на способ управления бэклогом через Prisma (утилита для режима разработка и интеграции с Cursor), без изменений логики самого агента. agent v1.5.0 (0 из 3980 строк core).

**2.3 В сервисе Телеграм-Бот**  
Без изменений (0 из 860 строк core). tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

**06.03.2026 >** Деплой: не блокировать `deploy-hoster-git` из‑за служебных файлов `.cursor/`; добавить cmd‑обёртку для стабильного запуска

**2.1 В приложении Комиссионка (web)**  
`scripts/deploy-hoster-git.ps1` больше не блокирует деплой, если рабочее дерево «грязное» **только** из‑за файлов Cursor в `.cursor/` (например, `.cursor/rules/*.mdc`). По умолчанию `.cursor/` игнорируется при проверке чистоты, чтобы деплой не стопорился из‑за служебных файлов IDE. Добавлены флаги: `-IgnoreCursor` (по умолчанию true) и `-AllowDirty` (на свой риск). Также добавлена обёртка `scripts/deploy-hoster-git.cmd` для запуска из cmd/двойного клика с `ExecutionPolicy Bypass`. Без изменений в core web: заменено 0 из 10246 строк. app v1.8.0 → v1.8.1 (патч — исправление препятствия деплоя).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений (0 из 3980 строк core). agent v1.5.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений (0 из 860 строк core). tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

**06.03.2026 >** Ведение бэклога: закладка в админке, таблица backlog в БД, дубликат в docs/backlog.md, инструмент агента во всех режимах

**2.1 В приложении Комиссионка (web)**  
Добавлена закладка «Бэклог» в админке с таблицей записей (порядковый №, № спринта, статус спринта: формируется/выполняется/реализован/архив, краткое описание, описание/промпт для ИИ — большое поле с Markdown, статус задачи: не начато/выполняется/тестируется/сделано/отказ, ссылка на документацию, порядок тестирования или ссылка на сценарии, даты создания и изменения). Добавление, редактирование (диалог) и удаление записей. При любом изменении таблица синхронизируется в [docs/backlog.md](docs/backlog.md). Новые файлы: [prisma/migrations/20260306120000_add_backlog/migration.sql](prisma/migrations/20260306120000_add_backlog/migration.sql), [src/app/api/admin/backlog/route.ts](src/app/api/admin/backlog/route.ts), [src/app/api/admin/backlog/[id]/route.ts](src/app/api/admin/backlog/[id]/route.ts), [src/lib/backlog-sync.ts](src/lib/backlog-sync.ts); модель backlog в [prisma/schema.prisma](prisma/schema.prisma); GET [src/app/api/admin/data/route.ts](src/app/api/admin/data/route.ts) возвращает backlog; правки в [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.7.2 → v1.8.0 (минор — бэклог).

**2.2 В сервисе Агент к модели ИИ**  
Инструмент `backlog` (action: list | create | update | delete) для чтения и записи бэклога через API приложения; доступен во всех режимах (курилка, консультация, разработка). Новый модуль [agent/tools/backlog-api.ts](agent/tools/backlog-api.ts), правки в [agent/tools/index.ts](agent/tools/index.ts) (executeTool, BACKLOG_TOOL в TOOLS_FOR_LLM, TOOLS_CHAT, TOOLS_CONSULT), в [agent/tools/run-command.ts](agent/tools/run-command.ts) в список разрешённых curl добавлены эндпоинты backlog. agent v1.4.10 → v1.5.0 (минор).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**05.03.2026 >** Ошибка агента: «tsx: not found» и падения при run_command; диагностика показала отсутствие node_modules/.bin в PATH дочернего процесса

**2.1 В приложении Комиссионка (web)**  
В [scripts/deploy-from-git.sh](scripts/deploy-from-git.sh) после миграций добавлен повторный вызов `npx prisma generate` для стабильной работы агента на сервере. Без изменений в core. app v1.7.2.

**2.2 В сервисе Агент к модели ИИ**  
В [agent/tools/run-command.ts](agent/tools/run-command.ts) при запуске дочернего процесса в окружение добавлен путь к корню проекта `node_modules/.bin` в начало PATH, чтобы в процессе находились `npx` и `tsx` (исправляет «tsx: not found» при выполнении разрешённых скриптов на сервере). agent v1.4.9 → v1.4.10 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**05.03.2026 >** При ошибке «Не удалось отправить запрос к агенту» выводить результаты выполнения диагностических команд на сервере

**2.1 В приложении Комиссионка (web)**  
При ошибке «Не удалось отправить запрос к агенту» и при 502 от API агента в чате админки следом за сообщением об ошибке автоматически запрашивается GET /api/admin/agent/diagnose: на сервере выполняются команды pm2 list, curl -s http://127.0.0.1:AGENT_PORT/health, pm2 logs agent --lines 100, их вывод возвращается и отображается в чате вторым сообщением («Результаты выполнения на сервере»). Новый роут [src/app/api/admin/agent/diagnose/route.ts](src/app/api/admin/agent/diagnose/route.ts), правки в [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.7.1 → v1.7.2 (минор — автоматический вывод диагностики агента).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.9.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**05.03.2026 >** При ошибке «Не удалось отправить запрос к агенту» выводить подсказку по диагностике

**2.1 В приложении Комиссионка (web)**  
При ошибке «Не удалось отправить запрос к агенту» (сетевой сбой, таймаут) и при ответе API 502 (агент недоступен) в чате админки сразу после текста ошибки выводится подсказка: проверка pm2 list, curl health на порт AGENT_PORT, pm2 logs agent --lines 100. [src/app/admin/page.tsx](src/app/admin/page.tsx). app v1.7.0 → v1.7.1 (патч — улучшение сообщения об ошибке агента).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.9.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**05.03.2026 >** Кнопка «Удалить товар» в режиме редактирования карточки товара

**2.1 В приложении Комиссионка (web)**  
В карточке товара в режиме редактирования добавлена кнопка «Удалить товар» (красная, с иконкой корзины). По нажатию показывается подтверждение; после удаления выполняется запрос DELETE к существующему API `/api/items/[id]` и редирект в «Мои объявления» (/seller). [src/app/items/[id]/ItemPageContent.tsx](src/app/items/[id]/ItemPageContent.tsx). app v1.6.0 → v1.7.0 (минор — новая кнопка удаления товара в режиме редактирования).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.9.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**05.03.2026 >** Генерация иллюстраций к товарам (Gemini Flash) и демо-данные с «семантическими» фото

**2.1 В приложении Комиссионка (web)**  
Добавлена серверная поддержка генерации иллюстраций к товарам через Gemini Flash: новый модуль `src/lib/item-image-generator.ts` использует SDK `@google/genai` и ключ `AGENT_LLM_API_KEY` из `.env`, генерирует изображения по названию и описанию товара и сохраняет их как реальные файлы в `public/uploads/items/` (пути `/uploads/items/*.png|jpg|webp` записываются в `items.image_url/image_urls`). Для карточки товара добавлен API-роут `src/app/api/items/[id]/generate-image/route.ts`, который по нажатию кнопки создаёт ещё одну иллюстрацию и добавляет её в массив `image_urls` (с автоматическим `revalidatePath` для главной и страницы товара). В режиме редактирования карточки (`ItemPageContent`) появилась кнопка «Сгенерировать новую иллюстрацию», доступная автору товара и администратору: она вызывает API, обновляет список фото и немедленно показывает новое изображение в галерее. Обновлена рабочая документация `docs/manual/app-komissionka.md` (секция 3.2, описание страницы товара и работы с картинками). В ядре web-приложения добавлено/изменено ~140 строк в 3 файлах (`src/app/items/[id]/ItemPageContent.tsx`, `src/app/api/items/[id]/generate-image/route.ts`, `src/lib/item-image-generator.ts`) из 9178 строк core (по `npx tsx scripts/count-core-lines.ts`). app v1.5.7 → v1.6.0 (новая фича — генерация иллюстраций к товарам).

Дополнительно обновлены сид-скрипты демо-данных: `scripts/seed-demo.ts` и `scripts/seed-five-users.ts` теперь при создании товаров генерируют по 3 «семантические» иллюстрации через тот же helper `generateItemImagesBatch` (название и описание товара попадают в промпт Gemini), а не используют статические плоские плейсхолдеры или внешние picsum-ссылки. При недоступности Gemini или отсутствии `AGENT_LLM_API_KEY` оба скрипта автоматически откатываются к безопасным плейсхолдерам `/images/placeholder.svg`, чтобы сиды оставались рабочими. Это гарантирует, что новые демо-товары сразу получают реалистичные изображения, совместимые с существующей системой раздачи `/api/uploads/items/[filename]`.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.9.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**05.03.2026 >** Режим разработка: разрешить удаление карточек товаров и пользователей (кроме bshevelev@mail.ru)

**2.1 В приложении Комиссионка (web)**  
Добавлен скрипт `scripts/cleanup-users-and-items.ts`: удаляет через Prisma все карточки товаров (items), связанные сообщения и корзины, активность и всех пользователей, кроме bshevelev@mail.ru. Используется агентом по запросу в режиме разработка. Без изменений в core web. app v1.5.7.

**2.2 В сервисе Агент к модели ИИ**  
В белый список разрешённых команд (описание в `getAllowedCommandsReadable`) и в системный промпт режима разработка добавлена команда `npx tsx scripts/cleanup-users-and-items.ts` — удаление всех карточек товаров и пользователей кроме bshevelev@mail.ru. Агент при таком запросе выполняет скрипт сам через run_command. [agent/tools/run-command.ts](agent/tools/run-command.ts), [agent/llm/system-prompt.ts](agent/llm/system-prompt.ts). agent v1.4.8 → v1.4.9 (минор — новая разрешённая команда в режиме разработка).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**05.03.2026 >** Точечная правка фото одного товара через picsum.photos

**2.1 В приложении Комиссионка (web)**  
Добавлен служебный скрипт `scripts/migrate-item-to-picsum.ts`, который по ID товара обновляет поля `image_url` и `image_urls` на три реальных изображения с `https://picsum.photos/seed/...` (одно основное и два дополнительных) на основе названия товара или переданного seed. Скрипт используется для точечного восстановления фото у конкретных объявлений (без затрагивания всей базы). В ходе работ им обновлён товар `Acoustic Drum Kit` (ID `99744016-8144-4b8a-a350-9cc0f4fb5ad8`). В ядре web-приложения добавлен один служебный файл (скрипт) из ~8905 строк core. app v1.5.6 → v1.5.7 (патч — точечное восстановление фото товара).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.8.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**05.03.2026 >** Демоданные: у товаров реальные фотографии вместо серых заглушек

**2.1 В приложении Комиссионка (web)**  
Обновлены скрипты демоданных для товаров: `scripts/seed-five-users.ts` теперь создаёт для каждого из 15 товаров по три фотореалистичных изображения с использованием `https://picsum.photos/seed/...` (одно «основное» и два дополнительных), а не плоские текстовые заглушки `placehold.co`. Скрипты `scripts/fix-placeholder-urls.ts` и `scripts/placeholder-to-files.ts` скорректированы так, чтобы не воспринимать `picsum.photos` как плейсхолдер и не переписывать такие URL в `/api/placeholder` или SVG-заглушки. Благодаря этому на главной странице и в карточках теперь показываются живые фотографии товаров, а не серые прямоугольники. В ядре web-приложения изменено 3 файла (скрипты и утилиты работы с картинками) из ~8900 строк core. app v1.5.5 → v1.5.6 (патч — исправление демо-фото каталога).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.8.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**05.03.2026 >** Сайт: восстанавливаем прод-сборку (ошибка TypeScript в admin data API)

**2.1 В приложении Комиссионка (web)**  
Исправлена ошибка TypeScript (`Parameter 'i' implicitly has an 'any' type`) в API-обработчике админки `/api/admin/data`, из-за которой `next build` падал на этапе проверки типов и не создавал полноценную прод-сборку (`.next/routes-manifest.json`, `BUILD_ID`). В маппинге списка товаров параметр `i` явно типизирован (разрешён `any`), чтобы сборка успешно проходила при текущей схеме Prisma и выборке полей. Это устраняет бесконечные рестарты `next start` и состояние, когда сайт не поднимается из-за отсутствия production build. [src/app/api/admin/data/route.ts](src/app/api/admin/data/route.ts). В ядре web-приложения 1 строка заменена из 8905 строк. app v1.5.4 → v1.5.5 (патч — фикс сборки).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.8.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Агент: устойчивость при вводе кода подтверждения (загрузка pending, структура плана)

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.5.4.

**2.2 В сервисе Агент к модели ИИ**  
При вводе кода подтверждения (например 9301): (1) вызов consumePending обёрнут в try/catch — при ошибке загрузки с диска возвращается сообщение с деталями вместо падения процесса; (2) перед выполнением плана проверяются toolCalls и filesToBackup (массивы, не пустые), при неверной структуре возвращается явная ошибка; (3) в цикле используются локальные переменные toolCalls/filesToBackup, чтобы не обращаться к полям, которые могли не восстановиться с диска. Это должно устранить «Не удалось отправить запрос к агенту» при обрыве из‑за исключения. [agent/core.ts](agent/core.ts). agent v1.4.7 → v1.4.8 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Агент: find_files принимает паттерн **/route.ts (поиск по всем разрешённым каталогам)

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.5.4.

**2.2 В сервисе Агент к модели ИИ**  
Инструмент find_files теперь принимает маску, начинающуюся с `**/` (например `**/route.ts`): поиск выполняется во всех разрешённых каталогах (src, prisma, docs, agent, public, telegram-bot, scripts, .cursor), результаты объединяются и возвращаются без дубликатов. Раньше такой паттерн отклонялся с ошибкой «Поиск разрешён только в каталогах...». [agent/tools/find-files.ts](agent/tools/find-files.ts). agent v1.4.6 → v1.4.7 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Агент: не падать при выполнении кода подтверждения; админка: не стирать ход выполнения при отправке сообщения

**2.1 В приложении Комиссионка (web)**  
В админке при отправке нового сообщения агенту больше не очищаются «Ход выполнения» и ссылка на лог: они заменяются при первом step/done текущего запроса, а при ошибке или обрыве запроса остаются от предыдущего ответа (можно скачать лог и смотреть шаги). [src/app/admin/page.tsx](src/app/admin/page.tsx): убраны вызовы setAiLastSteps([]) и setAiLastLogId(null) в начале отправки. app v1.5.3 → v1.5.4 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Выполнение подтверждённого плана (approval) обёрнуто в try/catch: при любой ошибке (например неверная структура toolCalls после загрузки с диска, падение run_command) агент возвращает сообщение об ошибке и не завершает процесс, поэтому клиент получает ответ вместо «Не удалось отправить запрос к агенту» (connection reset). При разборе аргументов tool call добавлена проверка typeof tc.arguments === "string". [agent/core.ts](agent/core.ts). agent v1.4.5 → v1.4.6 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Режим «разработка»: исправление «Неверный код подтверждения» — сериализация на диск и распознавание кода

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.5.3.

**2.2 В сервисе Агент к модели ИИ**  
Исправлена ситуация, когда код подтверждения (например 2277) не принимался сразу после показа плана. Причины: (1) при сохранении pending в файл JSON.stringify мог падать из‑за несериализуемых полей в messages (циклические ссылки и т.п.), запись молча не выполнялась, при следующем запросе (или другом процессе) код не находился; (2) промпт мог прийти в виде «код 2277» или с пробелами, а обрабатывался только строго «XXXX». Изменения: в [agent/approval-store.ts](agent/approval-store.ts) введена безопасная сериализация (pendingToSerializable / pendingFromSerializable): на диск пишется только сериализуемая копия (messages как массив { role, content, tool_calls }), при загрузке восстанавливается Pending; при ошибках save/load в консоль пишется лог. В [agent/core.ts](agent/core.ts) код подтверждения извлекается гибко: принимаются строка из четырёх цифр и варианты вида «код 2277», «Код: 2277», «подтверждаю 2277». В [agent/contract.ts](agent/contract.ts) обход кэша (forceFresh) для таких промптов расширен на те же форматы. agent v1.4.4 → v1.4.5 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Режим «разработка»: таймаут кода подтверждения считается от последнего поста модели с просьбой ввести код

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.5.3.

**2.2 В сервисе Агент к модели ИИ**  
Таймаут 30 минут для кода подтверждения теперь отсчитывается не от первого сообщения в кейсе (когда модель впервые показала план), а от **последнего** поста модели с просьбой ввести код («Подтвердите кодом: XXXX» или «Код подтверждения: XXXX»). Если пользователь уточняет план в диалоге и модель снова выводит тот же код — момент показа обновляется, и 30 минут даются на согласование после последней просьбы. В [agent/approval-store.ts](agent/approval-store.ts): добавлено поле `lastShownAt`, функция `refreshPendingShown(code)`; TTL везде считается от `ttlStart(p) = lastShownAt ?? createdAt`. В [agent/core.ts](agent/core.ts): при возврате ответа в режиме dev из текста извлекается код и вызывается `refreshPendingShown`. agent v1.4.3 → v1.4.4 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Режим «разработка»: коды подтверждения сохраняются на диск и не теряются при перезапуске агента

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.5.3.

**2.2 В сервисе Агент к модели ИИ**  
Хранилище кодов подтверждения (approval-store) переведено на персистентное хранение: состояние записывается в файл `.agent/pending-codes.json` и при старте агента подгружается оттуда. Код подтверждения остаётся действительным после перезапуска процесса (деплой, pm2 restart), поэтому ответ пользователя «2795» через несколько минут больше не приводит к «Неверный или несуществующий код подтверждения», если агент успел перезапуститься. Таймаут кода увеличен с 15 до 30 минут. В [agent/approval-store.ts](agent/approval-store.ts): loadStore/saveStore, ensureLoaded, обновлённые setPending/consumePending/hasPending. В [.gitignore](.gitignore) добавлен `/.agent/pending-codes.json`. agent v1.4.2 → v1.4.3 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Консультация и разработка: системный промпт только при первом сообщении в чате

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.5.3.

**2.2 В сервисе Агент к модели ИИ**  
В режимах «консультация» и «разработка» полный системный промпт отправляется в модель только при первом сообщении в чате (когда история реплик пуста). При каждом последующем сообщении в том же чате системный промпт не дублируется — модель опирается на контекст диалога. Это снижает расход токенов и засорение контекста. В логе по-прежнему фиксируется, был ли системный промпт включён в запрос. agent v1.4.1 → v1.4.2 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Режим «разработка»: ошибка COMMAND_DISALLOWED при запуске скриптов из scripts/

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.5.3.

**2.2 В сервисе Агент к модели ИИ**  
В белый список run_command добавлено разрешение на выполнение скриптов из каталога scripts/ с безопасным именем файла: `npx tsx scripts/<имя>.ts` (имя — только латиница, цифры, дефис, подчёркивание). Это устраняет ошибку COMMAND_DISALLOWED при запросах в режиме «разработка», когда модель предлагает выполнить, например, `npx tsx scripts/seed-five-users.ts` — ранее такая команда отклонялась и выполнение прерывалось. Восстановлен скрипт `scripts/seed-five-users.ts` (5 пользователей с паролем 123456, по 3 товара с 3 фото у каждого), приведён в соответствие со схемой Prisma (profiles.id/user_id, items). agent v1.4.0 → v1.4.1 (патч).

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Корзина: исправление ошибки 500 при добавлении товара

**2.1 В приложении Комиссионка (web)**  
Prisma-модель `cart_items` приведена в соответствие с фактической схемой БД: добавлено поле `id` как первичный ключ (`UUID`, `@id @default(uuid())`), а пара `user_id` + `product_id` переведена из составного первичного ключа в уникальный индекс (`@@unique([...], map: "cart_items_user_id_product_id_key")`). Ранее Prisma пытался выполнять `upsert` без явного значения `id`, а в таблице `cart_items` это поле помечено как NOT NULL без default, что приводило к ошибке `PrismaClientKnownRequestError P2011: Null constraint violation` при добавлении товара в корзину (`POST /api/cart`). Теперь Prisma генерирует UUID для `id` на стороне клиента и вставка проходит без ошибок, добавление товара в корзину работает корректно. app v1.5.2 → v1.5.3 (патч — фикс корзины).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Каталог: расстояние между карточками, отступ текста и размеры шрифтов — в админку

**2.1 В приложении Комиссионка (web)**  
В `site_settings` и во вкладку «Контент» админки добавлены параметры: расстояние между карточками каталога в px (`catalog_gap_px`, 8–64, по умолчанию 24), отступ текста внутри карточки от края в px (`catalog_card_padding_px`, 8–48, по умолчанию 24), размер шрифта заголовка карточки в px (`catalog_title_font_px`, 12–28, по умолчанию 18), размер шрифта текста карточки в px (`catalog_text_font_px`, 10–24, по умолчанию 14). Сетка каталога использует `gap` из настроек; карточки (ItemCardAnimated) применяют отступ и размеры шрифтов к заголовку и тексту. Миграция `20260304170000_add_catalog_card_display_params`. app v1.4.1 → v1.5.0 (новая фича).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Каталог: исправлены выбор 1/3 колонок в админке и отображение 4 колонок на главной

**2.1 В приложении Комиссионка (web)**  
Админка: поле «Минимальное число колонок» заменено на Select с опциями 1, 2, 3, 4 — значения 1 и 3 теперь явно доступны. API site-settings: при сохранении принимаются и число, и строка для `catalog_min_columns` (parseInt), обновление записывается при любом переданном значении. Главная: в catalog-grid.tsx для 1 колонки задаётся `grid-template-columns: 1fr`; для 2–4 колонок используется `repeat(N, minmax(min(maxCardWidth, calc((100% - (N-1)*24px) / N), 1fr))` с учётом gap-6 (24px), чтобы гарантированно отображалось ровно N колонок (раньше из‑за зазоров при 4 колонках показывало 3). app v1.4.0 → v1.4.1 (баг‑фикс).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Входные картинки для админки и Telegram‑бота (анализ изображений агентом)

**2.1 В приложении Комиссионка (web)**  
src/app/admin/page.tsx: во вкладке «Комиссионка AI» добавлена возможность прикрепить изображение к запросу (формат image/*, чтение через FileReader в data URL), запрос к /api/admin/agent/run теперь передаёт поле `inputImages` с массивом data URL; при отсутствии текста, но с вложением, промпт заполняется фразой «Проанализируй вложенное изображение…». Шапка админки адаптирована под мобильные устройства: кнопки «Обновить», «Prisma Studio», «Терминал», «Файлы» и «← На главную» на телефоне свёрнуты в компактное бургер‑меню справа от заголовка «Панель управления», чтобы не вылезать за экран. src/app/api/admin/agent/run/route.ts: проксирование `inputImages` в HTTP‑запрос к локальному агенту. Заменено ~70 строк из 8466 строк core. app v1.2.2 → v1.3.0 (новая фича — мультимодальный ввод в админке + мобильное меню шапки).

**2.2 В сервисе Агент к модели ИИ**  
agent/llm/client.ts, agent/llm/client-google.ts: в LLMRequestOptions добавлено поле `inputImages`, для Google Native (Gemini) входные изображения маппятся в parts.inlineData последнего пользовательского сообщения (mimeType + base64 без префикса), что соответствует формату generateContent; текстовые провайдеры OpenAI‑совместимого API не получают это поле в body. agent/contract.ts, agent/core.ts, agent/serve.ts: по цепочке от HTTP‑сервера `/run` до runAgentCore передаётся массив `{ mimeType, data }`, где data берётся из data URL (или уже готовой пары mime+base64); core прокидывает его в LLM‑клиент как `inputImages`. agent/image-dataurl-to-file.ts и существующая логика `responseModalitiesImage` остаются без изменений (работа только с исходящими data:image/...;base64,...). Заменено ~70 строк из 3702 строк core. agent v1.3.9 → v1.4.0 (новая фича — поддержка входных изображений для Gemini).

**2.3 В сервисе Телеграм-Бот**  
telegram-bot/bot.ts: сообщения с фото (`msg.photo`) больше не игнорируются — бот скачивает максимальное по размеру фото через Telegram API, кодирует в `data:image/jpeg;base64,...` и передаёт в агент через поле `inputImages` в POST `http://127.0.0.1:AGENT_PORT/run?stream=1`; текст промпта берётся из `msg.text` или подписи `caption`, при их отсутствии подставляется шаблон «Проанализируй вложенное изображение…». sendResultSegments и parseResultSegments по‑прежнему отправляют сгенерированные агентом картинки через sendPhoto (локальные файлы, /api/uploads/agent, data:image или http(s) URL). Обновлена документация [docs/manual/telegram-bot.md](docs/manual/telegram-bot.md) (разделы 3.4 и 3.6) и [docs/manual/agent.md](docs/manual/agent.md) по мультимодальному вводу. Заменено ~80 строк из 860 строк core. tgbot v1.0.8 → v1.1.0 (новая фича — анализ входных картинок в боте).

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Каталог на главной: параметры колонок и максимальной ширины карточек

**2.1 В приложении Комиссионка (web)**  
src/components/catalog-grid.tsx, src/components/HomeCatalogSection.tsx, src/services/homePageService.ts, src/app/page.tsx: параметры отображения каталога на главной странице вынесены в настройки сайта и админку. Вместо жёстко заданных констант теперь используются значения `catalog_min_columns` (минимальное количество колонок на небольших экранах, 1–4; по умолчанию 2) и `catalog_max_card_width` (максимальная ширина карточки товара, по умолчанию 360 пикселей), которые хранятся в `site_settings` и редактируются во вкладке «Контент» админ‑панели. Сетка строится через CSS `grid-template-columns: repeat(auto-fit, minmax(min(catalog_max_card_width, 100% / catalog_min_columns), 1fr))`, что гарантирует не менее указанного числа колонок на телефонах и ограничивает ширину карточек на широких мониторах. Обновлена документация [docs/manual/app-komissionka.md](docs/manual/app-komissionka.md) (раздел 3.2 — каталог и компоненты, раздел 3.5 — настройки сайта, описание модели `site_settings`). Заменено ~40 строк из 8601 строк core. app v1.3.2 → v1.4.0 (новая фича — управление параметрами сетки каталога из админки).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** GitHub Actions: отключён автодеплой, основной путь — только через deploy-hoster-git.ps1

**2.1 В приложении Комиссионка (web)**  
.github/workflows/deploy.yml: автозапуск деплоя при push в main (appleboy/ssh-action с выполнением npm ci, build и pm2 restart на сервере) отключён; workflow оставлен только с триггером workflow_dispatch как резервный ручной сценарий. Основной и единственный штатный путь деплоя зафиксирован: локальный запуск PowerShell-скрипта `scripts/deploy-hoster-git.ps1`, который вызывает на сервере `scripts/deploy-from-git.sh` (git fetch/reset, npm ci, prisma migrate deploy, next build, pm2 restart komissionka agent bot). Код приложения не менялся, только инфраструктура деплоя. app v1.3.0 → v1.3.1 (патч — правила и конфигурация деплоя).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**04.03.2026 >** Верхнее меню сайта: мобильный бургер‑профиль и иконки вместо длинных кнопок

**2.1 В приложении Комиссионка (web)**  
src/components/Navbar.tsx: переработано верхнее меню для мобильных устройств — слева добавлена компактная кнопка‑бургер «Меню пользователя» (открывает левый сайдбар с пунктами «Профиль», «Мои вещи», «Админ. Консоль» только для админов, «Выйти»), справа вместо длинных кнопок «Выставить вещь», «Админ. Консоль» и ФИО пользователя отображаются иконки: `+` (выставить вещь, /seller), «Мои вещи» (текстовая ссылка), колокольчик (уведомления — сейчас ведёт в профиль), корзина с бейджем количества, сердечко (избранное, /profile?tab=favorites). На десктопе функциональность сохраняется, но шапка на телефоне больше не уезжает вправо за пределы экрана. Заменено ~40 строк из 8466 строк core. app v1.3.1 → v1.3.2 (патч — улучшение мобильного UI шапки).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.4.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.1.0.

**2.4 PWA**  
Без изменений.

### ______***UPDATE***________

**03.03.2026 >** Git-деплой как единственный основной путь + восстановление загруженных изображений

**2.1 В приложении Комиссионка (web)**  
docs/manual/app-komissionka.md (раздел 7), docs/HOSTER-RU-DEPLOY.md, docs/DEPLOY-PUBLISH-CHANGES.md, .cursor/rules/chat-context.mdc и .cursor/rules/versioning.mdc приведены к единому сценарию: основной путь деплоя — только через GitHub и `.\scripts\deploy-hoster-git.ps1` → `scripts/deploy-from-git.sh` на сервере; старый `deploy-hoster.ps1` явно помечен как резервный (scp/rsync) и в обычном режиме не используется. При переходе на git-репозиторий на сервере восстановлены все ранее загруженные изображения (hero, items, chat, agent) копированием содержимого `public/uploads/*` из `komissionka_nongit_backup` в новый `~/komissionka/public/uploads` без перезаписи существующих файлов, чтобы не терять фото товаров и баннера. Заменено ~40 строк из 8409 строк core. app v1.2.1 → v1.2.2 (патч: документация деплоя + фиксация проблемы с пропавшими изображениями).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.9.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**03.03.2026 >** Git-деплой: фикс проверки статуса рабочего дерева в deploy-hoster-git.ps1

**2.1 В приложении Комиссионка (web)**  
scripts/deploy-hoster-git.ps1: устранена ошибка PowerShell «метод для выражения со значением NULL» при выполнении на чистом рабочем дереве — добавлена безопасная обработка пустого вывода `git status --porcelain`. Функциональность деплоя через GitHub и deploy-from-git.sh не менялась, исправлен только скрипт (патч). Заменено ~5 строк из 8409 строк core. app v1.2.0 → v1.2.1 (баг‑фикс в скрипте деплоя).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.9.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**03.03.2026 >** Личный кабинет пользователя: редактирование профиля и удаление аккаунта

**2.1 В приложении Комиссионка (web)**  
Добавлена страница личного кабинета `/profile` с формой редактирования ФИО, телефона, email, предпочтительного адреса поиска/ПВЗ и флага email‑уведомлений; отображаются дата регистрации и порядковый номер профиля, а также разделы истории заказов и отзывов (пока как заглушки). Имя пользователя в шапке (`Navbar`, `UserMenu`) теперь ведёт в личный кабинет. Расширена модель `profiles` (Prisma) полями `phone`, `preferred_location`, `email_notifications_enabled`, реализованы `GET/PATCH/DELETE` в `api/auth/profile` с безопасным обновлением email и удалением профиля (каскадно по связанным данным). Обновлена документация `docs/manual/app-komissionka.md` (блоки 3.1, 3.11, описание модели `profiles`). Заменено ~120 строк из 8409 строк core. app v1.1.1 → v1.2.0 (новая фича — личный кабинет пользователя).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.9.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**03.03.2026 >** Админка: защита от вечного «Думаю…» при обращении к агенту

**2.1 В приложении Комиссионка (web)**  
src/app/admin/page.tsx: при отправке запроса к /api/admin/agent/run добавлен таймаут 3 минуты через AbortController; при превышении таймаута пользователь получает явное сообщение, а индикатор «Думаю…» не зависает бесконечно. Заменено ~20 строк из 7758 строк core. app v1.1.0 → v1.1.1 (баг‑фикс).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.9.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**03.03.2026 >** Деплой через git: быстрый и надёжный вариант 1

**2.1 В приложении Комиссионка (web)**  
scripts/deploy-hoster-git.ps1: новый PowerShell-скрипт деплоя через git (git push origin → ssh hoster → bash scripts/deploy-from-git.sh). scripts/deploy-from-git.sh: серверный bash-скрипт, который делает git fetch/reset, npm ci, prisma generate/migrate deploy, npm run build, pm2 restart komissionka agent bot. Заменено ~60 строк в скриптах при общем объёме core 7758 строк. app v1.0.35 → v1.1.0 (новая фича — быстрый и надёжный git-деплой).

**2.2 В сервисе Агент к модели ИИ**  
AGENTS.md: обновлены инструкции — основной путь деплоя для агента теперь git-скрипт deploy-hoster-git.ps1 (старый deploy-hoster.ps1 оставлен как резервный scp/rsync). Код агента без изменений. agent v1.3.9.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**02.03.2026 >** Ошибка 401 «No cookie auth credentials found» при запросе к модели (OpenRouter)

**2.1 В приложении Комиссионка (web)**  
src/app/admin/page.tsx: при вызове /api/admin/agent/run добавлен credentials: "include", чтобы сессия (куки) всегда отправлялась. app v1.0.34 → v1.0.35.

**2.2 В сервисе Агент к модели ИИ**  
agent/core.ts: для OpenRouter используется ключ из modelOverride (от админки) или AGENT_OPENROUTER_API_KEY из .env; при отсутствии ключа — явное сообщение вместо 401 от API. Заменено ~12 строк. agent v1.3.8 → v1.3.9.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**02.03.2026 >** Агент: корректный роутинг Google/OpenRouter, удалена устаревшая модель gemini-2.5-pro

**2.1 В приложении Комиссионка (web)**  
src/lib/agent-models.ts: из списка доступных моделей в админке удалена недоступная модель `gemini-2.5-pro`, чтобы избежать 400 `"gemini-2.5-pro is not a valid model ID"`. Заменено 1 строка из 7758 строк core. app v1.0.33 → v1.0.34.

**2.2 В сервисе Агент к модели ИИ**  
agent/lib/model-utils.ts, agent/core.ts: роутинг по провайдерам теперь основан на `isOpenRouterModelId` (модели `anthropic/...`, `claude` идут через OpenRouter, остальные — через Google AI API). Для Google-моделей (Gemini/Gemma) запросы идут через AGENT_LLM_API_KEY/AGENT_LLM_BASE_URL, для OpenRouter-моделей — через AGENT_OPENROUTER_API_KEY/AGENT_OPENROUTER_BASE_URL. Исправлены ошибки 400 при выборе Gemini-моделей. Заменено ~15 строк из 3526 строк core. agent v1.3.7 → v1.3.8.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**02.03.2026 >** Курилка: чистый чат без системного промпта

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.33.

**2.2 В сервисе Агент к модели ИИ**  
agent/llm/system-prompt.ts, agent/core.ts: режим chat («курилка») больше не передаёт системный промпт и контекст в модель — только историю и текущий промпт пользователя. Документация обновлена (docs/manual/agent.md вер. 1.3.7). agent v1.3.6 → v1.3.7.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**02.03.2026 >** Документация app: версия в заголовке 1.0.19 → 1.0.33

**2.1 В приложении Комиссионка (web)**  
docs/manual/app-komissionka.md: заголовок приведён к version.json (1.0.33). app v1.0.33.

**2.2–2.4** Без изменений.

---

**02.03.2026 >** Деплой самостоятельно + таблица дат ключевых файлов в подвале

**2.1 В приложении Комиссионка (web)**  
Правила: деплой — строго самостоятельно, никогда не писать «убедитесь, что задеплоено». Подвал: таблица дат ключевых файлов на сервере (скрипт server-key-files-dates.ps1, .sh). Обновлены: versioning.mdc, chat-context.mdc, AGENTS.md, VERSIONING-RULES.md. app v1.0.33.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**02.03.2026 >** Правила версионности: распознавание фича/баг и подвал отчёта

**2.1 В приложении Комиссионка (web)**  
Без изменений кода. Обновлены правила: .cursor/rules/versioning.mdc, AGENTS.md, docs/VERSIONING-RULES.md, chat-context.mdc — добавлено требование явно распознавать задание как фичу или баг; в подвале отчёта указывать «Задание распознано как: фича(и) [и/или баг(и)]». app v1.0.33.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**02.03.2026 >** Deploy: копирование docs, manual, scripts, agent, .cursor/rules на сервер

**2.1 В приложении Комиссионка (web)**  
deploy-hoster.ps1: убрано исключение .cursor — правила и документация (.cursor/rules, docs/, docs/manual/, scripts/, agent/, telegram-bot/) копируются на сервер для модели ИИ. Добавлено исключение .next (сборка). app v1.0.32 → v1.0.33.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**02.03.2026 >** Фото товаров: реальные файлы вместо /api/placeholder в БД

**2.1 В приложении Комиссионка (web)**  
Скрипт placeholder-to-files.ts: для товаров с /api/placeholder в image_urls создаёт SVG-файлы в public/uploads/items/ и обновляет БД. api/uploads/items: поддержка SVG. recreate-items.ts: создаёт реальные файлы, а не API-URL. app v1.0.31 → v1.0.32.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**02.03.2026 >** Админка: кнопки «Терминал» и «Файлы», исправление загрузки в файловом менеджере

**2.1 В приложении Комиссионка (web)**  
src/app/admin/page.tsx: добавлены кнопки «Терминал» (→ /admin/terminal) и «Файлы» (→ /admin/files) в шапку панели управления. src/app/admin/files/page.tsx: исправлен upload — useRef вместо useState для file input, корректный вызов click(). app v1.0.30 → v1.0.31.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**02.03.2026 >** Админка: чаты ИИ в IndexedDB (до 4ГБ, до ручного удаления)

**2.1 В приложении Комиссионка (web)**  
src/lib/ai-chats-storage.ts: IndexedDB для чатов «Комиссионка AI» с fallback на localStorage. Миграция из localStorage при первом запуске. При QuotaExceeded — удаление старейших чатов. Сохранение с debounce 300 мс. app v1.0.29 → v1.0.30.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**02.03.2026 >** Фото товаров (попытка 12): плейсхолдер без сетевого запроса

**2.1 В приложении Комиссионка (web)**  
item-card-animated: для URL-плейсхолдеров (/api/placeholder, /images/placeholder) использовать встроенный PLACEHOLDER_DATA_URI напрямую — без запроса к серверу. Гарантированное отображение. app v1.0.28 → v1.0.29.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**02.03.2026 >** Prisma Studio: редирект на главную при открытии в новой вкладке

**2.1 В приложении Комиссионка (web)**  
Auth-provider: loading включает загрузку профиля (profileLoadAttempted). При открытии /admin/prisma-studio в новой вкладке profile ещё не загружен, userRole=null — не редиректить до получения profile. app v1.0.27 → v1.0.28.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Фото товаров: радикальное решение — API-плейсхолдер, rewrite, диагностика

**2.1 В приложении Комиссионка (web)**  
Фото товаров на главной показывали «Нет фото» (10 попыток). Причина: статика public/images/ в production может быть недоступна. Решение: rewrite /images/placeholder.svg → /api/placeholder; recreate-items и fix-placeholder-urls используют /api/placeholder; новая страница /diagnose-images и API /api/diagnose-images для диагностики. app v1.0.26 → v1.0.27.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Админка «Комиссионка AI»: мобильная вёрстка для смартфона

**2.1 В приложении Комиссионка (web)**  
Вкладка «Комиссионка AI»: на смартфоне чат отображается первым, ниже — сессии (горизонтальный скролл) и блок «Ход выполнения». Адаптивные высоты, выбор модели корректно переносится. app v1.0.25 → v1.0.26.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Uploads 404: API-маршруты для hero, items, chat — файлы после сборки

**2.1 В приложении Комиссионка (web)**  
Next.js в production не отдаёт статику из public/, добавленную после build. Добавлены API /api/uploads/hero/[filename], items/[filename], chat/[filename] + rewrites /uploads/* → /api/uploads/*. Баннер и фото товаров загружаются через route handlers. app v1.0.24 → v1.0.25.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Кнопка Prisma Studio в админке — встроенная схема данных без пароля

**2.1 В приложении Комиссионка (web)**  
@prisma/studio-core + postgres: встроенный Prisma Studio в /admin/prisma-studio. API /api/admin/studio с проверкой admin. Кнопка «Prisma Studio» в шапке админки, открывается в новой вкладке. Доступ по сессии (без ввода пароля БД). Новая фича → минор. app v1.0.23 → v1.1.0.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Документация: глава Deploy в каждой системе

**2.1 В приложении Комиссионка (web)**  
docs/manual/app-komissionka.md, agent.md, telegram-bot.md: добавлена глава Deploy (схема разработка→прод, Dev/Prod пути, инкрементальный и полный деплой). Без изменений версии.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Deploy: инкрементальная синхронизация, пропуск неизменённых файлов

**2.1 В приложении Комиссионка (web)**  
scripts/deploy-hoster.ps1: rsync (PATH/WSL/Chocolatey) — пропуск неизменённых; fallback — PowerShell-синк по размеру файлов, retry 3× при сбое. choco install rsync — для быстрого деплоя. app v1.0.22 → v1.0.23.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** crypto.randomUUID fallback для старых браузеров / HTTP

**2.1 В приложении Комиссионка (web)**  
lib/session-id.ts: fallback для crypto.randomUUID (HTTP, старые браузеры). UUID v4 через Math.random при отсутствии Web Crypto API. app v1.0.21 → v1.0.22.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Heartbeat 401: вызывать только для авторизованных

**2.1 В приложении Комиссионка (web)**  
ActivityProvider: heartbeat вызывается только при status === "authenticated". Устранены 401 Unauthorized для гостей в консоли. app v1.0.20 → v1.0.21.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Фото товаров (попытка 7): статический /images/placeholder.svg, mixed content fix

**2.1 В приложении Комиссионка (web)**  
public/images/placeholder.svg — статический SVG. resolveImageUrl: /api/ и /images/ оставляем относительными (избежание mixed content). seed-demo: /images/placeholder.svg. scripts/fix-placeholder-urls.ts — миграция битых URL в БД. app v1.0.19 → v1.0.20.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Фото товаров: API /api/placeholder, data URI fallback — без внешних сервисов

**2.1 В приложении Комиссионка (web)**  
API /api/placeholder — SVG-плейсхолдер на своём сервере. seed-demo: /api/placeholder вместо placehold.co. lib/placeholder.ts — PLACEHOLDER_DATA_URI. item-card, ItemPageContent: img с data URI при ошибке. next/image заменён на img для фото. app v1.0.18 → v1.0.19.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Агент: детальный план изменений, поиск по всем каталогам

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.18.

**2.2 В сервисе Агент к модели ИИ**  
core.ts: getCommandDescription(), getWriteFileDescription() — детальное описание каждого run_command и write_file в плане изменений (что именно выполнят скрипты). path-safe: добавлены scripts/, .cursor/ в ALLOWED_DIR_PREFIXES. grep: поиск по всем каталогам (src, prisma, docs, agent, public, telegram-bot, scripts, .cursor). find_files: то же. system-prompt и tools: обновлены описания. agent v1.3.5 → v1.3.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Картинки товаров: fallback при битых ссылках, placehold.co .png

**2.1 В приложении Комиссионка (web)**  
item-card-animated: onError → fallback «Фото не загружается». ItemPageContent: onError на Image. seed-demo: placehold.co с .png для совместимости. app v1.0.17 → v1.0.18.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Контекст обновлён: docs/manual, исправление hhtps→https, документация по модулям

**2.1 В приложении Комиссионка (web)**  
Создан каталог docs/manual/ с рабочей документацией. docs/manual/README.md — индекс. docs/manual/app-komissionka.md — приложение v1.0.17: стек, архитектура, функциональные блоки со ссылками на код, модель данных, доступ к Prisma, HTTPS для ЛК. Опечатка hhtps→https в .cursor/rules/chat-context.mdc. app v1.0.17 (без изменений кода).

**2.2 В сервисе Агент к модели ИИ**  
docs/manual/agent.md — агент v1.3.5: стек, архитектура, блоки (run, serve, core, tools, cache), системный промпт и экономия токенов. agent v1.3.5 (без изменений кода).

**2.3 В сервисе Телеграм-Бот**  
docs/manual/telegram-bot.md — бот v1.0.8: стек, архитектура, привязка профилей, вызов агента, управление. tgbot v1.0.8 (без изменений кода).

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** OOM fix: NODE_OPTIONS=512MB, ecosystem.config.cjs, setup-swap, docs/OOM-FIX.md

**2.1 В приложении Комиссионка (web)**  
package.json: start с NODE_OPTIONS=--max-old-space-size=512. ecosystem.config.cjs — PM2 с лимитами памяти. scripts/setup-swap-hoster.ps1, switch-to-ecosystem-pm2.ps1. docs/OOM-FIX.md. app v1.0.17.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Картинки: placehold.co вместо picsum, resolveImageUrl для /uploads/, next.config

**2.1 В приложении Комиссионка (web)**  
seed-demo: picsum.photos → placehold.co. src/lib/image-url.ts — resolveImageUrl для относительных путей. homePageService, items/[id]: подстановка APP_BASE_URL. next.config: placehold.co, komissionka92.ru в remotePatterns. app v1.0.16 → v1.0.17.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** check-services-status: app по HTTP /api/health вместо lsof

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.16.

**2.2 В сервисе Агент к модели ИИ**  
scripts/check-services-status.ts: проверка app через curl к http://127.0.0.1:3000/api/health вместо lsof (надёжнее на сервере). agent v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Логин и товары: trustHost, NEXTAUTH_URL=http, unstable_noStore, window.location после входа

**2.1 В приложении Комиссионка (web)**  
auth: trustHost для reverse proxy. login: проверка result?.ok, window.location.href вместо router для гарантированного обновления сессии. page.tsx, items/[id]: unstable_noStore против кэша. setup-domain: NEXTAUTH_URL=http (пока без SSL). scripts/diagnose-db.ts. app v1.0.15 → v1.0.16.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**01.03.2026 >** Хостинг: ensure-admin, seed-demo без пользователей, домен komissionka92.ru, архив локального проекта

**2.1 В приложении Комиссионка (web)**  
scripts/ensure-admin.ts — создание/обновление админа bshevelev@mail.ru. scripts/seed-demo.ts — перезаливка только items, news, testimonials (пользователей не трогает). scripts/restore-db-hoster.ps1, setup-domain-komissionka92.ps1, archive-local-and-stop-db.ps1. scripts/db-check.ts — подсчёт записей по таблицам. docs/DB-MANUAL-INSPECT.md, docs/PRISMA-AUDIT.md. app v1.0.14 → v1.0.15.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**27.02.2026 >** Рефакторинг: force-dynamic, revalidatePath — устранение рассинхронизации БД и UI

**2.1 В приложении Комиссионка (web)**  
page.tsx: dynamic после импортов. items/[id]/page.tsx: force-dynamic уже был. Admin API: revalidatePath("/") в site-settings PATCH, news POST/DELETE, testimonials POST/PATCH/DELETE, items POST, items/[id] DELETE/PATCH. app v1.0.13 → v1.0.14.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**27.02.2026 >** Админка: фото баннера Hero подтягивается с origin

**2.1 В приложении Комиссионка (web)**  
admin: img Hero-изображения — как у картинок агента: подстановка window.location.origin для относительных путей, поддержка путей без ведущего слеша. app v1.0.12 → v1.0.13.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

**27.02.2026 >** Товар не найден: GET /api/items/[id], decodeURIComponent, диагностика на странице

**2.1 В приложении Комиссионка (web)**  
api/items/[id]: добавлен публичный GET для проверки наличия товара (404 + totalItems при ненайден). items/[id]/page: decodeURIComponent(id), подсказка «откройте /api/items/{id}» при «не найден». scripts/check-items-db.ts — скрипт диагностики БД. app v1.0.11 → v1.0.12.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

### 28.02.2026 > Картинки в админке: rsync сохраняет uploads, UPLOADS_AGENT_DIR обязателен

**28.02.2026 >** Деплой: rsync с --exclude=public/uploads — картинки агента не перезаписываются. scp fallback — предупреждение. admin: img с origin для абсолютного URL. Документация: UPLOADS_AGENT_DIR обязателен.

**2.1 В приложении Комиссионка (web)**  
deploy-hoster: rsync при наличии, exclude public/uploads. admin: img src с window.location.origin. docs: UPLOADS_AGENT_DIR. app v1.0.10 → v1.0.11.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.8.

**2.4 PWA**  
Без изменений.

---

### 28.02.2026 > Рефакторинг: пул БД, getLatestItems, localhost, бот 409, uploads

**28.02.2026 >** prisma.ts: DB_POOL_MAX, DB_POOL_IDLE_TIMEOUT. itemService: один запрос с include users.profiles. bot.ts, agent/config: в production требуют APP_BASE_URL, AGENT_APP_URL. bot: 409 → process.exit(1). upload/item: TODO S3, проверка S3_BUCKET. .env.example.

**2.1 В приложении Комиссионка (web)**  
src/lib/prisma.ts: max, idleTimeoutMillis. src/services/itemService.ts: include users.profiles, один запрос. src/app/api/upload/item: TODO S3, 501 при S3_BUCKET. .env.example. app v1.0.9 → v1.0.10.

**2.2 В сервисе Агент к модели ИИ**  
agent/config.ts: AGENT_APP_URL обязателен в production. agent v1.3.4 → v1.3.5.

**2.3 В сервисе Телеграм-Бот**  
APP_BASE_URL обязателен в production. 409 Conflict → process.exit(1). tgbot v1.0.7 → v1.0.8.

**2.4 PWA**  
Без изменений.

---

### 28.02.2026 > restart-services.ts в архив, перезапуск через PM2

**28.02.2026 >** Локальные службы не используются (режим только сервер). scripts/restart-services.ts перемещён в scripts/archive/. Агент использует pm2 restart komissionka agent bot.

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.9.

**2.2 В сервисе Агент к модели ИИ**  
agent/core.ts: runRestartThenGetStatus — pm2 restart. agent/tools/run-command.ts: белый список pm2 вместо restart-services. agent/llm/system-prompt.ts: инструкция pm2. agent v1.3.3 → v1.3.4.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.7.

**2.4 PWA**  
Без изменений.

---

### 28.02.2026 > Диагностика: 404 изображений, чат, товар не найден, БД

**28.02.2026 >** Добавлен /api/health для диагностики (БД, uploads, cwd). API uploads/agent — поддержка UPLOADS_AGENT_DIR. Чат — beforeunload для сохранения. Страницы / и /items/[id] — force-dynamic. deploy-hoster — исключение .env при загрузке. docs/DIAGNOSTIC-SERVER.md, HOSTER-RU-DEPLOY — инструкции.

**2.1 В приложении Комиссионка (web)**  
src/app/api/health/route.ts — диагностика. api/uploads/agent — UPLOADS_AGENT_DIR. admin: beforeunload для persist чата. items/[id]: проверка id, force-dynamic. page: force-dynamic. deploy-hoster: exclude .env. app v1.0.8 → v1.0.9.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.3.3.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.7.

**2.4 PWA**  
Без изменений.

---

### 28.02.2026 > Системный промпт для Nano Banana (курилка), кэш URL, фото в админке

**28.02.2026 >** Для моделей с генерацией изображений в режиме курилка — убран лишний блок «Жёсткие правила» (инструменты, write_docs_file). Промпт сокращён: модель только генерирует картинки. Из кэша при ответе: нормализация /uploads/agent/ → /api/uploads/agent/ для старых записей.

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.8.

**2.2 В сервисе Агент к модели ИИ**  
agent/llm/system-prompt.ts: для isImageModel в chat — упрощённый промпт без «Жёсткие правила». agent/contract.ts: при ответе из кэша — замена ](/uploads/agent/ на ](/api/uploads/agent/. agent v1.3.2 → v1.3.3.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.7.

**2.4 PWA**  
Без изменений.

---

### 28.02.2026 > Вторая картинка в чате ИИ: вместо изображения — placeholder «generated»

**28.02.2026 >** Второе и последующие изображения в чате с ИИ иногда не отображались (показывался alt «generated»). Обработка data URL в agent/image-dataurl-to-file.ts усилена: санитизация base64 (пробелы, URL-safe -/_), гибкий regex для data URL, проверка размера буфера.

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.8.

**2.2 В сервисе Агент к модели ИИ**  
agent/image-dataurl-to-file.ts: regex с \s* для data URL, санитизация base64 (strip whitespace, URL-safe -→+, _→/), проверки b64.length и buf.length. agent v1.3.1 → v1.3.2.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.7.

**2.4 PWA**  
Без изменений.

---

### 28.02.2026 > Месторасположение: СЕРВЕР; PM2; fix PostgreSQL; esbuild; ответ с картинкой

**28.02.2026 >** Документация: репозиторий на сервере. PM2 agent+bot, fix-postgres-auth, setup-pm2-all. agent: system-prompt template→array.join (esbuild fix). Для ответов с изображениями — requestSummary «Финальный ответ: изображение» вместо дампа base64.

**2.1 В приложении Комиссионка (web)**  
src/app/api/uploads/agent/[filename]/route.ts — раздача изображений агента. app v1.0.7 → v1.0.8.

**2.2 В сервисе Агент к модели ИИ**  
agent: system-prompt template→array.join, core — requestSummary/лог без base64. agent/image-dataurl-to-file: URL изменён на /api/uploads/agent/ для раздачи через API. agent v1.3.0 → v1.3.1.

**2.3 В сервисе Телеграм-Бот**  
Поддержка /api/uploads/agent/ в sendResultSegments. tgbot v1.0.6 → v1.0.7.

**2.4 PWA**  
Без изменений.

---

### 27.02.2026 > Агент: работа с репозиторием на новом сервере

**27.02.2026 >** Агент адаптирован для работы с приложением на прод-сервере (Hoster.ru VPS). URL API задаётся через AGENT_APP_URL.

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.6.

**2.2 В сервисе Агент к модели ИИ**  
agent/config.ts: appUrl из AGENT_APP_URL (уже было). agent/tools/run-command.ts: белый список curl и подстановка X-Agent-API-Key для любого appUrl (не только localhost). agent/llm/system-prompt.ts: промпт с динамическим URL. agent/tools/index.ts: описание run_command с URL из конфига. agent/config.json.example: appUrl. docs/HOSTER-RU-DEPLOY.md: раздел «Агент ИИ на сервере». agent v1.2.6 → v1.3.0.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.6.

**2.4 PWA**  
Без изменений.

---

### 28.02.2026 > Отказ от mobile-app в пользу PWA

**28.02.2026 >** Удалены mobile-app (Expo/React Native), API /api/mobile/*, docs/MOBILE-APP-SETUP.md. Стабилизация: tsconfig exclude, скрипты deploy/setup.

**2.1 В приложении Комиссионка (web)**  
Удалены: mobile-app/, src/app/api/mobile/. Исправлены: agent-models-help (ImageIcon title), ItemPageContent (currentIdx null). tsconfig: убран mobile-app из exclude. app v1.0.5 → v1.0.6.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.2.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.6.

**2.4 PWA**  
Мобильное приложение удалено. PWA — часть web-приложения (адаптивная вёрстка).

---

### 27.02.2026 > tgbot: не принимался код подтверждения в режиме разработка

**27.02.2026 >** Код 4 цифр и «откат NNNN» в Telegram не срабатывали — из кэша возвращался старый ответ.

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.5.

**2.2 В сервисе Агент к модели ИИ**  
agent/contract.ts: обход кэша для промптов вида «5269» и «откат 5269» в режиме dev. Иначе findSimilar возвращал устаревший ответ «Неверный код» вместо проверки approval-store. agent v1.2.5 → v1.2.6.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.6.

**2.4 PWA**  
Без изменений.

---

### 27.02.2025 > Чек-лист тестирования и промпт для Gemini

**27.02.2025 >** Чек-лист всех функций 3 приложений (Комиссионка, Агент, Телеграм-Бот) в табличном виде; технический промпт для Gemini по созданию папки «Vibe coding projects» (Внешний диск), подпапок проектов, Google Таблицы с общим доступом, выпадающими списками и автозаполнением.

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.5.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.2.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.6.

**2.4 PWA**  
Без изменений. (Планируется к реализации.)

Новые объекты: [docs/TESTING-CHECKLIST-PROMPT.md](docs/TESTING-CHECKLIST-PROMPT.md) (полный промпт для Gemini), [docs/testing-checklist-data.csv](docs/testing-checklist-data.csv) (53 строки для импорта).

---

### 26.02.2025 18:00 > Структура лога what's new.md

**26.02.2025 18:00 >**

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.5.

**2.2 В сервисе Агент к модели ИИ**  
Унифицирована структура лога. Обновлены [docs/VERSIONING-RULES.md](docs/VERSIONING-RULES.md), [.cursor/rules/versioning.mdc](.cursor/rules/versioning.mdc). agent v1.2.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.6.

**2.4 PWA**  
Без изменений. (Планируется к реализации.)

---

### 26.02.2025 > Автоперезапуск перед подвалом, статус служб в подвале

**26.02.2025 >** Автоперезапуск перед подвалом, статус служб в подвале.

**2.1 В приложении Комиссионка (web)**  
Без изменений. app v1.0.5.

**2.2 В сервисе Агент к модели ИИ**  
Новые объекты: [scripts/check-services-status.ts](scripts/check-services-status.ts), [agent/lib/services-status.ts](agent/lib/services-status.ts). Изменены: agent/core.ts, agent/contract.ts, agent/lib/report-footer.ts. Заменено ~80 строк из 3419 core. Обновлён [AGENTS.md](AGENTS.md). agent v1.2.4 → v1.2.5.

**2.3 В сервисе Телеграм-Бот**  
Без изменений. tgbot v1.0.6.

**2.4 PWA**  
Без изменений. (Планируется к реализации.)

---

### 26.02.2025 — tgbot: fix image delivery

**2.1 В приложении Комиссионка (web)**  
Без изменений.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений.

**2.3 В сервисе Телеграм-Бот**  
RegEx update + error msg truncation (base64 spam prevention). tgbot v1.0.5 → v1.0.6.

**2.4 PWA**  
Без изменений.

---

### 26.02.2025 — agent-models: typeIcon для перечня моделей

**2.1 В приложении Комиссионка (web)**  
Иконки типов моделей (📝🖼️📦) в админке. app v1.0.4 → v1.0.5.

**2.2 В сервисе Агент к модели ИИ**  
Без изменений.

**2.3 В сервисе Телеграм-Бот**  
Без изменений.

**2.4 PWA**  
Без изменений.

---

### 26.02.2025 — API agent-log: last-reasoning.txt

**2.1 В приложении Комиссионка (web)**  
Без изменений.

**2.2 В сервисе Агент к модели ИИ**  
last-reasoning.txt вместо .log (Telegram открывает в чате).

**2.3 В сервисе Телеграм-Бот**  
Без изменений.

**2.4 PWA**  
Без изменений.

---

### 26.02.2025 — AGENTS.md, versioning.mdc: запрет забывать подвал

**2.1 В приложении Комиссионка (web)**  
Без изменений.

**2.2 В сервисе Агент к модели ИИ**  
Обновлены [AGENTS.md](AGENTS.md), [.cursor/rules/versioning.mdc](.cursor/rules/versioning.mdc). Усилен запрет забывать подвал отчёта.

**2.3 В сервисе Телеграм-Бот**  
Без изменений.

**2.4 PWA**  
Без изменений.

---

### ______***UPDATE***________

**19.03.2026 >** Багфикс: маппинг модели `Gemini 3` для выполнения scope=agent в тестах

**2.1 В приложении Комиссионка (web)**  
Исправлено выполнение тест‑кейса scope=`agent`: нормализация `parameters.model` теперь конвертирует сокращение `"Gemini 3"` в валидный model id `gemini-3-pro-preview`, чтобы агенту не уходили неподдерживаемые имена и Gemini API не возвращал `400 unexpected model name format`. Файл: [src/app/api/admin/test-cases/[id]/run/route.ts](src/app/api/admin/test-cases/[id]/run/route.ts). Заменено строк core: 5 из 13486. app v1.16.7 → v1.16.8 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

**19.03.2026 >** Багфикс: обогащение спецификации сохраняет `parameters` из любого формата ответа ИИ

**2.1 В приложении Комиссионка (web)**  
Усилена обработка ответа ИИ в кнопке «Обогатить спецификацию тест‑кейса с ИИ»: если `model/mode/userPrompt/expectedText` вернулись либо внутри `parameters`, либо на корневом уровне JSON, UI всё равно сформирует `parameters` и сохранит их в `test_cases` через `PUT /api/admin/test-cases`. Это предотвращает ситуации, когда после обогащения параметры остаются `{}` и прогон падает по `expectedTextMissing`. Файл: [src/components/AdminTestCatalogTab.tsx](src/components/AdminTestCatalogTab.tsx). Заменено строк core: 12 из 13481. app v1.16.6 → v1.16.7 (патч).

**2.2 В сервисе Агент к модели ИИ**  
Без изменений. agent v1.7.8.

**2.3–2.4**  
Без изменений. tgbot v1.1.0.

---

### 26.02.2025 — v1.0.0 Стартовый отсчёт

Правила версионности, Cursor rules, what's new. app v1.0.0, agent v1.0.0, tgbot v1.0.0.
