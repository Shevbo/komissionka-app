______***UPDATE***________
18.03.2026 — v1.7.4 — Force-fresh (!) теперь гарантирует пересылку системного промпта: при запросах с '!' агент принудительно отправляет системный промпт в LLM (актуальный контекст и правила инструментов), чтобы не зависеть от «памяти модели».
______***UPDATE***________
18.03.2026 — v1.7.3 — Фикс пересылки системного промпта: при повторных ошибках в чате последним сообщением может быть ответ без подвала с версиями, поэтому версия теперь ищется по всей истории ассистента (последнее упоминание agent vX.Y.Z), чтобы после деплоя гарантированно обновлялся контекст модели.
______***UPDATE***________
18.03.2026 — v1.7.2 — read_file теперь поддерживает offset_lines/limit_lines (чтение фрагментов больших файлов без sed/head/tail), а agent/core.ts повторно отправляет системный промпт после смены версии агента в истории (чтобы модель не оставалась в старом контексте после деплоя).
______***UPDATE***________
17.03.2026 — v1.7.1 — Исправлена ложная пометка tool-ошибок: read_file/grep больше не считаются "ошибкой" из-за слова Error внутри содержимого файла; ошибка определяется только по префиксу вида "[tool] error:" / "[error]".
______***UPDATE***________
17.03.2026 — v1.7.0 — Уточняющие вопросы перед изменениями и строгие правила логов: в системном промпте режима разработка агент обязан почти всегда сначала задавать хотя бы один уточняющий вопрос (кроме совсем простых запросов), выяснять файлы/части системы и способ проверки результата перед вызовом write_file/run_command; правила ведения what's new.md вынесены в явный блок (общий и модульные логи, актуальная дата, версии были → стали).
______***UPDATE***________
17.03.2026 — v1.6.1 — Этапы 3–4: политика прав и снятие pending approval (agent/core.ts, agent/contract.ts, agent/tools/run-command.ts, agent/llm/system-prompt.ts). Логика подтверждения вторым сообщением и кода убрана, политика прав вынесена в промпт, run_command блокирует опасные команды.
______***UPDATE***________
17.03.2026 — v1.6.0 — Этапы 1–2: агент без внешнего подтверждения («да»/код), обновлён системный промпт режима разработка, нормализатор ответа в /api/admin/agent/run больше не требует второго сообщения.
______***UPDATE***________
17.03.2026 — v1.5.9 — EADDRINUSE на порту агента: обработчик server.on(\"error\") в agent/serve.ts, EADDRINUSE добавлен в retry в /api/admin/agent/run.
______***UPDATE***________
26.02.2025 — v1.2.5 — Автоперезапуск перед подвалом, статус служб в подвале. check-services-status.ts, agent/lib/services-status.ts. Core: app 6380, agent 3419, tgbot 803.
______***UPDATE***________
26.02.2025 — v1.2.4 — Image-модель: промпт «любой запрос = картинка» в курилке/consult/dev. Иконки типов моделей (📝🖼️📦).
______***UPDATE***________
26.02.2025 — v1.2.3 — agent/lib/model-utils.ts: импорт parseModelIdWithModality из agent, а не из src (обход ошибки при tsx agent/serve).
______***UPDATE***________
26.02.2025 — v1.2.2 — План одним пакетом, строгая логика X.Y.Z, перезапуск сервисов, last-reasoning.txt, count-core-lines.ts.
______***UPDATE***________
26.02.2025 — v1.2.0 — Накопительное обновление: синхронизация версий (agent/package.json), фиксация множественных изменений в ядре и инструментах.
______***UPDATE***________
26.02.2025 — v1.1.1 — Prisma adapter в cache, load .env.local, [[РЕЖИМ]] в промпте, stripPrefixChain (цепочка префиксов), forceFresh для "!" без пробела.
______***UPDATE***________
26.02.2025 — v1.1.0 — Автоперезапуск сервисов после изменения кода (restart-services.ts, таймаут 15 с).
______***UPDATE***________
26.02.2025 — v1.0.0 — Стартовый отсчёт версий. Правила версионности.
______***UPDATE***________

# Изменения агента
