# Архив скриптов

Скрипты, утратившие актуальность после перехода на режим «только сервер».

## restart-services.ts

Локальный перезапуск (npm run dev, agent:serve, bot:start) по портам.
**Не используется** — перезапуск на сервере: `pm2 restart komissionka agent bot`.
